---
phase: 01-landing-editorial-funda-o
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - site/components/sections/ParaQuemE.tsx
  - site/components/sections/Metodo.tsx
  - site/components/sections/Resultados.tsx
  - site/components/sections/FAQ.tsx
autonomous: true
requirements: [RDSN-01, DSGN-01, DSGN-03]
must_haves:
  truths:
    - "Um visitante vê ParaQuemE, Metodo, Resultados e FAQ redesenhadas no padrão editorial sóbrio, monocromático estrito"
    - "Exatamente 1 bloco recebe fundo branco pontual (D-01) usando os tokens fg-on-light/muted-on-light (texto secundário ≥4.5:1 sobre branco)"
    - "Metodo aparece como passos editoriais numerados (re-diagramação à discrição do planner)"
    - "Com prefers-reduced-motion ativo, o accordion do FAQ abre instantaneamente (sem animar height)"
    - "Nenhuma seção, exceto FAQ, carrega 'use client'"
  artifacts:
    - path: "site/components/sections/Metodo.tsx"
      provides: "Metodo re-diagramada como passos editoriais numerados"
      contains: "font-display"
    - path: "site/components/sections/FAQ.tsx"
      provides: "FAQ com gate de reduced-motion no accordion"
      contains: "useReducedMotion"
  key_links:
    - from: "site/components/sections/FAQ.tsx"
      to: "framer-motion useReducedMotion"
      via: "gate do accordion"
      pattern: "useReducedMotion"
    - from: "white-block (Metodo)"
      to: "globals.css tokens"
      via: "text sobre fundo branco via muted-on-light/fg-on-light"
      pattern: "muted-on-light|fg-on-light"
---

<objective>
Redesenhar as quatro seções não-foto-conduzidas — ParaQuemE, Metodo, Resultados e FAQ — no padrão editorial sóbrio monocromático, reconciliando a escala tipográfica para os tetos contidos do contrato, re-diagramando o Metodo como passos editoriais numerados (discrição do planner), introduzindo o único bloco de fundo branco pontual da página (D-01) sobre os tokens de contraste-sobre-branco da fundação, e fechando o gate de reduced-motion que falta — o accordion do FAQ (Framer height-animate). Resultados mantém o AnimatedCounter (já gateado no Plano 01).

Purpose: Completa RDSN-01 (as 7 seções no padrão editorial) para o conjunto que não depende de foto, e fecha o último gap de DSGN-03 (accordion do FAQ). É a fatia que torna o miolo da landing coerente com o Hero foto-conduzido do Plano 02.
Output: ParaQuemE/Metodo/Resultados/FAQ redesenhadas; 1 bloco branco pontual; FAQ gateado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-editorial-funda-o/01-CONTEXT.md
@.planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md
@.planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md
@.planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md
@.planning/phases/01-landing-editorial-funda-o/01-01-SUMMARY.md

<interfaces>
<!-- Fundação do Plano 01 que este plano consome. -->

Reveal (site/components/Reveal.tsx): { children, delay?, className? } — já gateado para reduced-motion (Plano 01)
AnimatedCounter (site/components/AnimatedCounter.tsx): { to, duration?, prefix?, suffix? } — já gateado (Plano 01); MANTER o uso em Resultados (D-09)

Tokens disponíveis (globals.css, Plano 01):
  bg #0a0a0a, fg #ededed, muted #7f7f7f (só sobre preto), surface #141414, line #262626
  fg-on-light #0a0a0a (texto primário sobre branco), muted-on-light #595959 (texto secundário sobre branco, ≥4.5:1)
  REGRA DURA: NUNCA usar text-muted (#7f7f7f) sobre fundo branco (dá ~3.5:1, falha AA). Sobre branco usar fg-on-light / muted-on-light.

Padrão de grid editorial canônico do codebase (Metodo/Resultados atuais):
  `grid gap-px overflow-hidden border border-line bg-line sm:grid-cols-N` com células `h-full bg-bg p-8`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Redesenhar ParaQuemE + Metodo (passos numerados) com 1 bloco branco pontual</name>
  <files>site/components/sections/ParaQuemE.tsx, site/components/sections/Metodo.tsx</files>
  <read_first>
    - site/components/sections/ParaQuemE.tsx (estado atual: H2 md:text-6xl; dois cards "É pra você se"/"Não é pra você se" com lucide Check/X; Reveal stagger)
    - site/components/sections/Metodo.tsx (estado atual: H2 md:text-6xl; 4 PRINCIPIOS em grid de células bg-bg com ícones lucide; lista INCLUI em chips border-line; Reveal stagger)
    - .planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md §Typography (H2 text-4xl→sm:text-5xl, H3 text-2xl) + §Design Tokens (60/30/10, branco como bloco pontual, tokens sobre-branco) + §Spacing
    - .planning/phases/01-landing-editorial-funda-o/01-CONTEXT.md (Claude's Discretion: Metodo como passos editoriais numerados; quais blocos recebem branco pontual)
    - .planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md entrada Metodo/ParaQuemE (cell-grid; um destes é candidato a white-block)
  </read_first>
  <action>
    Ambas permanecem Server Components (sem "use client"). Reconciliar a escala tipográfica do H2 nas duas seções de `md:text-6xl` para a escala do contrato: base `text-4xl` (36px) com `sm:text-5xl` (48px). Manter os ícones lucide com `aria-hidden`, o padrão de grid editorial e o Reveal stagger.
    ParaQuemE: manter os dois cards (É pra você / Não é pra você) — refinar para o tom editorial sóbrio (respiro, hierarquia), sem adicionar seção nem mudar a copy (já on-brand). Manter sobre fundo preto/surface (NÃO é o bloco branco).
    Metodo (re-diagramação — discrição do planner conforme CONTEXT): transformar os 4 PRINCIPIOS de cards-com-ícone em PASSOS EDITORIAIS NUMERADOS — cada princípio prefixado por um numeral display Anton ("01", "02", "03", "04") em `font-display`, com título (`text-2xl` H3) e texto ao lado/abaixo num ritmo editorial. Pode manter os ícones lucide como detalhe secundário ou removê-los em favor do numeral (decisão do executor pelo tom contido). Preservar os 4 PRINCIPIOS e a lista INCLUI (chips) — sem inventar conteúdo.
    BLOCO BRANCO PONTUAL (D-01 — EXATAMENTE 1 na página inteira; este plano hospeda-o): dar fundo branco (`bg-white`) ao bloco da lista INCLUI ("O que compõe a consultoria") do Metodo — é o respiro de destaque natural. NESSE bloco branco, o texto primário usa `text-[var(--color-fg-on-light)]` e o secundário `text-[var(--color-muted-on-light)]` (#595959) — NUNCA `text-muted`/#7f7f7f. As bordas dos chips dentro do bloco branco devem ser escuras (ex.: `border-black/15` ou `border-[var(--color-fg-on-light)]/20`) para contraste. É o único respiro branco; branco é exceção intencional, não base. Registrar no SUMMARY que o bloco branco vive na lista INCLUI do Metodo, para que ParaQuemE/Resultados/FAQ NÃO adicionem outro.
  </action>
  <verify>
    <automated>cd "site" && ! grep -q "use client" components/sections/ParaQuemE.tsx && ! grep -q "use client" components/sections/Metodo.tsx && ! grep -q "md:text-6xl" components/sections/ParaQuemE.tsx && ! grep -q "md:text-6xl" components/sections/Metodo.tsx && grep -Eq "muted-on-light|fg-on-light" components/sections/Metodo.tsx && grep -q "bg-white" components/sections/Metodo.tsx && npm run lint && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/sections/ParaQuemE.tsx` e `Metodo.tsx` NÃO contêm `"use client"`
    - Nenhuma das duas contém `md:text-6xl`; H2 usa `text-4xl` com `sm:text-5xl`
    - `site/components/sections/Metodo.tsx` exibe os 4 princípios com numerais display ("01".."04") em `font-display`
    - `site/components/sections/Metodo.tsx` tem o bloco da lista INCLUI com `bg-white`, texto via `var(--color-fg-on-light)`/`var(--color-muted-on-light)`, e NÃO usa `text-muted` dentro do bloco branco
    - `npm run lint` e `npx tsc --noEmit` passam
    - Comportamento: bloco branco do Metodo legível, texto secundário ≥4.5:1 (medir com contrast checker)
  </acceptance_criteria>
  <done>ParaQuemE refinada; Metodo re-diagramada em passos numerados com o único bloco branco pontual da página em tokens sobre-branco; ambas RSC; lint+typecheck verdes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Redesenhar Resultados (escala) + gate de reduced-motion no accordion do FAQ</name>
  <files>site/components/sections/Resultados.tsx, site/components/sections/FAQ.tsx</files>
  <read_first>
    - site/components/sections/Resultados.tsx (estado atual: H2 md:text-6xl; grid sm:grid-cols-3 de STATS com AnimatedCounter em font-display text-6xl md:text-7xl; bloco placeholder de depoimentos border-dashed)
    - site/components/sections/FAQ.tsx (estado atual: "use client" legítimo — useState do accordion; Framer AnimatePresence anima height 0↔auto + opacity; a11y aria-expanded/aria-controls/role=region presente)
    - .planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md §Typography (H2 sm:text-5xl; Numeral/metric text-5xl→sm:text-6xl) + §States & Interactions (gate reduced-motion por lib)
    - .planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md §Code Examples (gate Framer via useReducedMotion linhas ~352-360) + §Pitfall 3
    - .planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md entradas Resultados.tsx e FAQ.tsx
  </read_first>
  <action>
    Resultados permanece Server Component. Reconciliar escala: H2 `md:text-6xl` → `sm:text-5xl`; o numeral do AnimatedCounter de `text-6xl md:text-7xl` para a escala de métrica do contrato `text-5xl` (48px) com `sm:text-6xl` (60px). MANTER o uso de `AnimatedCounter` (D-09 — reuso explícito; o gate já vive dentro dele desde o Plano 01) e a fonte de dados `STATS` de lib/site.ts. Manter o bloco placeholder de depoimentos (border-dashed) — seções de conversão/depoimentos reais são Fase 2; refinar visualmente para o tom, sem inventar conteúdo nem remover o placeholder. NÃO adicionar bloco branco (o único vive no Metodo — Task 1).
    FAQ continua `"use client"` (legítimo — accordion precisa de useState; é a única seção client permitida, DSGN-05). Adicionar o gate de reduced-motion (DSGN-03, último gap): importar `useReducedMotion` de framer-motion; quando `reduce` for true, o painel aberto deve aparecer INSTANTANEAMENTE sem animar height — renderizar o conteúdo do painel sem o `motion.div` animado (ou com `transition` desativada / `initial=false` e sem animação de height), de modo que o toggle não dispare a animação de altura. Sob `no-preference`, manter o `AnimatePresence` + `motion.div` height 0↔auto atual (animar height aqui é a exceção permitida do accordion, mas reduced-motion DEVE pulá-la). Preservar toda a a11y existente (aria-expanded/aria-controls/role=region/aria-labelledby) e o estado `open`. Reconciliar H2 `md:text-6xl` → `sm:text-5xl`.
  </action>
  <verify>
    <automated>cd "site" && ! grep -q "use client" components/sections/Resultados.tsx && grep -q "AnimatedCounter" components/sections/Resultados.tsx && ! grep -q "md:text-6xl" components/sections/Resultados.tsx && head -1 components/sections/FAQ.tsx | grep -q "use client" && grep -q "useReducedMotion" components/sections/FAQ.tsx && ! grep -q "md:text-6xl" components/sections/FAQ.tsx && npm run lint && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/sections/Resultados.tsx` NÃO contém `"use client"`; mantém `AnimatedCounter` e `STATS`; H2 sem `md:text-6xl`; numeral usa `text-5xl`/`sm:text-6xl`
    - `site/components/sections/Resultados.tsx` mantém o bloco placeholder de depoimentos (não publica conteúdo inventado)
    - `site/components/sections/FAQ.tsx` importa `useReducedMotion` de framer-motion e tem branch que abre o painel sem animar height quando reduce=true
    - `site/components/sections/FAQ.tsx` preserva aria-expanded/aria-controls/role="region"/aria-labelledby e o estado `open`
    - Nenhuma das duas adiciona `bg-white` (bloco branco único vive no Metodo)
    - `npm run lint` e `npx tsc --noEmit` passam
    - Comportamento: com `prefers-reduced-motion: reduce` emulado, abrir um item do FAQ → painel aparece instantâneo, sem animação de altura; contador mostra valor final estático
  </acceptance_criteria>
  <done>Resultados com escala reconciliada e AnimatedCounter preservado; FAQ accordion gateado para reduced-motion mantendo a11y; ambas no padrão editorial; lint+typecheck verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → CDN/browser | Renderização estática de seções de conteúdo; FAQ tem estado de UI client-side (accordion) sem input externo |

Slice frontend-only. Sem auth, sem formulário, sem persistência, sem input de usuário (o accordion alterna estado de UI local). Superfície de ameaça mínima.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-XSS | Tampering | conteúdo de seção (FAQ/STATS/textos) | accept | Todo conteúdo é estático no código (arrays literais); sem `dangerouslySetInnerHTML`, sem input de usuário — N/A |
| T-03-TRACK | Info Disclosure | TrackingScripts | accept | Fora de escopo (Fase 3); este plano não toca `TrackingScripts.tsx` |
| T-03-CLIENT | Information Disclosure | FAQ "use client" | accept | Única seção client (accordion); só estado de UI local, sem dados sensíveis — N/A |
</threat_model>

<verification>
- `cd site && npm run lint && npx tsc --noEmit` verde após cada task
- `cd site && npm run build` verde ao fim; ler "First Load JS" da rota `/` e confirmar <200KB (DSGN-02)
- `grep -rL "use client" components/sections app/page.tsx` mostra ParaQuemE/Metodo/Resultados como server (só FAQ é client) — DSGN-05
- Manual: rodar `npm run dev`; bloco branco do Metodo legível com texto secundário ≥4.5:1 (contrast checker); Metodo em passos numerados; demais seções monocromáticas sóbrias
- Manual reduced-motion: accordion do FAQ abre instantâneo; contador estático
</verification>

<success_criteria>
- ParaQuemE/Metodo/Resultados/FAQ redesenhadas no padrão editorial monocromático, escala tipográfica reconciliada
- Metodo em passos editoriais numerados; único bloco branco pontual (lista INCLUI) em tokens sobre-branco com AA
- FAQ accordion gateado para reduced-motion mantendo a11y; AnimatedCounter preservado em Resultados
- Apenas FAQ é client; demais seções server
- build + lint + typecheck verdes; First Load JS <200KB
</success_criteria>

<output>
Create `.planning/phases/01-landing-editorial-funda-o/01-03-SUMMARY.md` when done. Registrar: confirmação de que o bloco branco vive no Metodo (lista INCLUI), o contraste medido do texto secundário sobre branco, e o First Load JS da rota `/`.
</output>
