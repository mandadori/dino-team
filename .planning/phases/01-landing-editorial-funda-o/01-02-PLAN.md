---
phase: 01-landing-editorial-funda-o
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - site/components/sections/Hero.tsx
  - site/components/sections/SobreRamon.tsx
  - site/components/sections/CtaFinal.tsx
  - site/app/page.tsx
autonomous: true
requirements: [RDSN-01, RDSN-02, DSGN-01]
must_haves:
  truths:
    - "Um visitante vê o Hero foto-conduzido com a foto P&B do Ramon full-bleed (ou placeholder intencional) + scrim, H1 = sign-off da marca, CTA 'Quero minha direção'"
    - "Um visitante vê o SobreRamon com o retrato P&B vertical do Ramon (ou placeholder) + scrim, substituindo o gradiente"
    - "O título display do Hero não passa de lg:text-7xl (teto contido D-02), nunca text-8xl"
    - "O texto sobre a foto permanece legível (≥4.5:1) graças ao scrim aplicado"
    - "Hero, SobreRamon e CtaFinal permanecem Server Components (motion só via islands importados)"
  artifacts:
    - path: "site/components/sections/Hero.tsx"
      provides: "Hero editorial foto-conduzido com RamonPhoto + scrim + parallax + copy reconciliada"
      contains: "RamonPhoto"
    - path: "site/components/sections/SobreRamon.tsx"
      provides: "SobreRamon com retrato RamonPhoto + scrim-portrait"
      contains: "RamonPhoto"
  key_links:
    - from: "site/components/sections/Hero.tsx"
      to: "site/components/RamonPhoto.tsx"
      via: "import + src=/ramon/hero.jpg"
      pattern: "RamonPhoto"
    - from: "site/components/sections/Hero.tsx"
      to: "site/components/motion/ParallaxImage.tsx"
      via: "import (parallax do Hero)"
      pattern: "ParallaxImage"
---

<objective>
Redesenhar o núcleo foto-conduzido da landing — Hero, SobreRamon e CtaFinal — no padrão editorial sóbrio, substituindo os placeholders de gradiente por foto P&B full-bleed do Ramon (via RamonPhoto, com placeholder intencional enquanto o acervo não chega), aplicando os scrims obrigatórios, baixando o teto tipográfico de display para a escala contida (D-02), e reconciliando a copy divergente para os literais do contrato (sign-off, eyebrow, CTA). É a fatia que entrega a alma visual da marca (RDSN-02) e prova end-to-end a fundação do Plano 01 (RSC → island de motion → reduced-motion gate → contraste medido sobre foto).

Purpose: Hero e SobreRamon são os alvos canônicos de RDSN-02 (fotos reais P&B); CtaFinal fecha a página com o sign-off da marca. Esta fatia torna o site visivelmente foto-conduzido — o maior salto perceptível da fase.
Output: Hero/SobreRamon/CtaFinal redesenhados; copy do topo (header) reconciliada em page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-editorial-funda-o/01-CONTEXT.md
@.planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md
@.planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md
@.planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md
@.planning/phases/01-landing-editorial-funda-o/01-01-SUMMARY.md

<interfaces>
<!-- Componentes-fundação criados no Plano 01 que este plano consome. -->

RamonPhoto (site/components/RamonPhoto.tsx, criado no Plano 01):
  props { src?: string; alt: string; priority?: boolean; scrim: "hero" | "portrait"; className?: string }
  - src presente → next/image fill grayscale contrast-125 + overlay de scrim (token CSS)
  - src ausente → placeholder monocromático intencional "Foto do Ramon" (role="img"+aria-label)

ParallaxImage (site/components/motion/ParallaxImage.tsx, criado no Plano 01):
  client island; envolve children (foto); parallax leve GSAP gateado por gsap.matchMedia; estático sob reduced-motion

CTAButton (site/components/ui/CTAButton.tsx): { href, children, variant?, className? } — já com hover scale CSS + focus ring (Plano 01)
Reveal (site/components/Reveal.tsx): { children, delay?, className? } — já gateado para reduced-motion (Plano 01)
WHATSAPP_URL (site/lib/site.ts): placeholder wa.me/0000000000 — NÃO renomear, NÃO trocar (WhatsApp real é Fase 2)

Tokens disponíveis (globals.css, Plano 01): bg #0a0a0a, fg #ededed, muted #7f7f7f, surface #141414, line #262626, fg-on-light #0a0a0a, muted-on-light #595959, var(--scrim-hero), var(--scrim-portrait)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Redesenhar Hero foto-conduzido (RamonPhoto + parallax + scrim + tipografia + copy)</name>
  <files>site/components/sections/Hero.tsx, site/app/page.tsx</files>
  <read_first>
    - site/components/sections/Hero.tsx (estado atual: section relative min-h-[100svh]; placeholder radial-gradient aria-hidden + watermark DINO; eyebrow "Consultoria de treino e dieta"; H1 "O método do campeão, aplicado em você." em md:text-8xl; lead; CTA "Quero minha consultoria"; stagger Reveal delay 0.08/0.16/0.24)
    - site/app/page.tsx (header com CTAButton label "Quero minha consultoria"; composição das 7 seções; footer)
    - site/components/RamonPhoto.tsx (componente-fundação do Plano 01)
    - site/components/motion/ParallaxImage.tsx (island do Plano 01)
    - .planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md §Typography (H1 teto lg:text-7xl, line-height 0.95) + §Copywriting (literais) + §Design Tokens (scrim)
    - .planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md §Pattern 2 (foto full-bleed + scrim) + §Pattern 4 (corrigir teto tipográfico) + §Runtime State Inventory item 2 (copy divergente) + §Pitfall 6 (priority/LCP)
    - .planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md entrada Hero.tsx (changes 1-4)
  </read_first>
  <action>
    Hero permanece SERVER COMPONENT — não adicionar "use client" (motion só via islands importados — DSGN-05, RESEARCH Pitfall 4). Substituir o placeholder radial-gradient (o `<div aria-hidden ...>` de fundo) por `<RamonPhoto src="/ramon/hero.jpg" alt="Ramon Dino em preto e branco, alto contraste" priority scrim="hero" className="absolute inset-0" />` posicionado como camada de fundo full-bleed da `section relative min-h-[100svh] overflow-hidden`. Envolver essa foto de fundo no island `<ParallaxImage>` (Hero é o alvo de parallax — D-08; parallax leve, sem pin). O conteúdo de texto (eyebrow/H1/lead/CTA) fica numa camada `relative z-10` ACIMA do scrim. Manter o watermark DINO opcional (aria-hidden) se ainda fizer sentido visual, ou removê-lo agora que há foto — decisão do executor pelo tom; documentar.
    Reconciliar copy (literais do contrato, RESEARCH item 2 + UI-SPEC §Copywriting): eyebrow → "Consultoria Ramon Dino" (uppercase, tracking largo); H1 → o sign-off da marca "O topo exige direção." (literal, sem variação); manter o lead atual (já on-brand) ou refinar levemente; CTA label → "Quero minha direção".
    Tipografia (D-02, Pattern 4): baixar o teto do H1 de `md:text-8xl` para `lg:text-7xl` (72px teto); escala `text-5xl` (48px) base → `sm:text-6xl`? Não — seguir UI-SPEC: base `text-5xl`, teto `lg:text-7xl`, line-height ~0.95 (`leading-[0.95]`). NÃO usar text-8xl em lugar nenhum.
    Em `site/app/page.tsx`: reconciliar o label do CTAButton do header de "Quero minha consultoria" para "Quero minha direção" (consistência de CTA — UI-SPEC §Copywriting reusa o mesmo verbo-noun). NÃO mexer no `WHATSAPP_URL` nem em `TrackingScripts`. O texto do footer pode ser refinado para o tom, mas não é obrigatório — manter coerência monocromática.
  </action>
  <verify>
    <automated>cd "site" && grep -q "RamonPhoto" components/sections/Hero.tsx && grep -q "ParallaxImage" components/sections/Hero.tsx && grep -q "O topo exige direção" components/sections/Hero.tsx && grep -q "lg:text-7xl" components/sections/Hero.tsx && ! grep -q "text-8xl" components/sections/Hero.tsx && ! grep -q "use client" components/sections/Hero.tsx && grep -q "Quero minha direção" app/page.tsx && npm run lint && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/sections/Hero.tsx` importa e usa `RamonPhoto` com `src="/ramon/hero.jpg"`, `priority`, `scrim="hero"`, e o envolve em `ParallaxImage`
    - `site/components/sections/Hero.tsx` contém `lg:text-7xl` e NÃO contém `text-8xl`
    - `site/components/sections/Hero.tsx` H1 é o literal "O topo exige direção."; eyebrow "Consultoria Ramon Dino"; CTA "Quero minha direção"
    - `site/components/sections/Hero.tsx` NÃO contém `"use client"`
    - `site/app/page.tsx` CTA do header é "Quero minha direção"; `WHATSAPP_URL` e `TrackingScripts` intocados
    - Comportamento: sem foto em public/ramon/, o Hero renderiza o placeholder intencional "Foto do Ramon" (não quebra, não gradiente colorido); com foto, full-bleed P&B + scrim
    - `npm run lint` e `npx tsc --noEmit` passam
  </acceptance_criteria>
  <done>Hero foto-conduzido com parallax leve, scrim, teto tipográfico contido e copy reconciliada; header CTA consistente; Hero permanece RSC; lint+typecheck verdes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Redesenhar SobreRamon (retrato RamonPhoto + scrim-portrait) e CtaFinal (sign-off)</name>
  <files>site/components/sections/SobreRamon.tsx, site/components/sections/CtaFinal.tsx</files>
  <read_first>
    - site/components/sections/SobreRamon.tsx (estado atual: grid md:grid-cols-2; slot de retrato role="img" aspect-[4/5] gradiente + watermark; H2 "Do Acre ao topo do mundo." em md:text-6xl; TIMELINE map com Reveal stagger; fecho display md:text-3xl)
    - site/components/sections/CtaFinal.tsx (estado atual: H2 centralizado md:text-7xl + lead + CTAButton com Reveal stagger)
    - site/components/RamonPhoto.tsx (Plano 01)
    - .planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md §Typography (H2 text-4xl→sm:text-5xl) + §Copywriting (sign-off, CTA) + §Design Tokens (scrim-portrait)
    - .planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md entradas SobreRamon.tsx e CtaFinal.tsx
    - .planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md §Pattern 2 (retrato + scrim-portrait)
  </read_first>
  <action>
    SobreRamon permanece RSC. Substituir o slot de retrato (o `<div role="img" ... aspect-[4/5] ... gradiente ...>`) por `<RamonPhoto src="/ramon/retrato.jpg" alt="Retrato de Ramon Dino em preto e branco" scrim="portrait" className="relative aspect-[4/5] ..." />` — preservar o `aspect-[4/5]` e a moldura `border border-line`. RamonPhoto já cuida do `role="img"`/aria-label e do placeholder intencional quando `src` ausente. Sem `priority` (não é LCP — fica lazy). Opcional/discrição do executor: este é o segundo candidato a parallax (D-08, UI-SPEC linha 103) — pode envolver a foto em `ParallaxImage` se ajudar o tom, mas NÃO é obrigatório; se incluir, parallax leve e degradar em mobile.
    Reconciliar tipografia do H2 de `md:text-6xl` para a escala UI-SPEC `text-4xl` base → `sm:text-5xl` (48px teto da seção). Manter o map de TIMELINE com Reveal stagger (já gateado no Plano 01). Manter a copy biográfica atual (on-brand, ancorada no brand book). O fecho display pode ser refinado para o tom contido (não ampliar escala).
    CtaFinal permanece RSC. Reconciliar: pode hospedar o sign-off "O topo exige direção." como H2 (UI-SPEC linha 148 — alternativa ao Hero; se o Hero já usa como H1, CtaFinal pode usar outro fecho on-brand sem repetir literalmente — decisão do executor para não duplicar o sign-off na mesma página; documentar). CTA label → "Quero minha direção". H2 atual `md:text-7xl` está perto do teto — confirmar contra UI-SPEC (H2 de seção é text-4xl→sm:text-5xl; CtaFinal é um fecho de impacto, pode usar escala maior que H2 padrão, mas NÃO ultrapassar o teto de display lg:text-7xl). Manter centralização e Reveal stagger.
  </action>
  <verify>
    <automated>cd "site" && grep -q "RamonPhoto" components/sections/SobreRamon.tsx && grep -q "retrato.jpg" components/sections/SobreRamon.tsx && grep -q "scrim=\"portrait\"" components/sections/SobreRamon.tsx && ! grep -q "use client" components/sections/SobreRamon.tsx && grep -q "Quero minha direção" components/sections/CtaFinal.tsx && ! grep -q "text-8xl" components/sections/CtaFinal.tsx && npm run lint && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/sections/SobreRamon.tsx` usa `RamonPhoto` com `src="/ramon/retrato.jpg"` e `scrim="portrait"`, preserva `aspect-[4/5]`, sem `priority`
    - `site/components/sections/SobreRamon.tsx` H2 reconciliado para `sm:text-5xl` (sem `md:text-6xl`); NÃO contém `"use client"`
    - `site/components/sections/CtaFinal.tsx` CTA é "Quero minha direção"; escala display não ultrapassa `lg:text-7xl`; NÃO contém `text-8xl`; permanece RSC
    - Comportamento: SobreRamon sem foto → placeholder intencional no slot 4/5; com foto → retrato P&B + scrim-portrait
    - `npm run lint` e `npx tsc --noEmit` passam
  </acceptance_criteria>
  <done>SobreRamon com retrato foto-conduzido + scrim e tipografia reconciliada; CtaFinal fecha com CTA consistente e escala contida; ambas RSC; lint+typecheck verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → CDN/browser | Renderização estática de seções foto-conduzidas; sem input, sem auth, sem datastore |
| public/ramon/ → next/image | Fotos servidas same-origin (mesma origem do site) |

Slice frontend-only de redesign de seções. Sem superfície de autenticação, sem formulário, sem persistência. Superfície de ameaça mínima.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-IMG | Tampering | next/image (RamonPhoto no Hero/SobreRamon) | mitigate | Fotos só de `public/ramon/` same-origin; `images.remotePatterns` permanece fechado; `alt` estático on-brand no código |
| T-02-TRACK | Info Disclosure | TrackingScripts / WHATSAPP_URL | accept | Fora de escopo (Fase 2/3); este plano não toca `TrackingScripts.tsx` nem renomeia `NEXT_PUBLIC_WHATSAPP_URL` |
| T-02-XSS | Tampering | conteúdo de seção | accept | Texto/alt estáticos no código; sem `dangerouslySetInnerHTML`, sem input — N/A |
</threat_model>

<verification>
- `cd site && npm run lint && npx tsc --noEmit` verde após cada task
- `cd site && npm run build` verde ao fim; ler "First Load JS" da rota `/` e confirmar <200KB (DSGN-02) — registrar no SUMMARY
- `grep -rL "use client" components/sections app/page.tsx` permanece limpo (Hero/SobreRamon/CtaFinal/page seguem server) — DSGN-05
- Manual: rodar `npm run dev`; sem foto → placeholders intencionais "Foto do Ramon" no Hero/SobreRamon; com foto colocada em public/ramon/ → full-bleed P&B + scrim; medir contraste do texto sobre a foto na região mais clara (≥4.5:1, critério de aceite 2)
- Manual reduced-motion: parallax do Hero não dispara; reveals estáticos
</verification>

<success_criteria>
- Hero foto-conduzido: RamonPhoto full-bleed + scrim + parallax leve, H1 sign-off, CTA "Quero minha direção", teto lg:text-7xl
- SobreRamon: retrato RamonPhoto + scrim-portrait substituindo o gradiente, tipografia reconciliada
- CtaFinal: fecho on-brand, CTA consistente, escala contida
- Hero/SobreRamon/CtaFinal/page.tsx permanecem Server Components
- build + lint + typecheck verdes; First Load JS <200KB
</success_criteria>

<output>
Create `.planning/phases/01-landing-editorial-funda-o/01-02-SUMMARY.md` when done. Registrar: First Load JS medido, qualquer ajuste de opacidade de scrim para atingir AA sobre a foto, e se o segundo parallax (SobreRamon) foi incluído ou não.
</output>
