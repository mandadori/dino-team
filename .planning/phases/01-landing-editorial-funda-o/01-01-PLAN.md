---
phase: 01-landing-editorial-funda-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/app/globals.css
  - site/lib/usePrefersReducedMotion.ts
  - site/components/Reveal.tsx
  - site/components/AnimatedCounter.tsx
  - site/components/ui/CTAButton.tsx
  - site/components/RamonPhoto.tsx
  - site/components/motion/ParallaxImage.tsx
  - site/package.json
  - site/package-lock.json
  - site/public/ramon/README.md
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05]
must_haves:
  truths:
    - "Com prefers-reduced-motion ativo, Reveal e AnimatedCounter renderizam estado final estático (sem animação)"
    - "@gsap/react está instalado e disponível para useGSAP()"
    - "D-03: contraste AA tratado nos blocos brancos (token #595959 sobre branco) e nos overlays sobre foto; #7f7f7f mantido sobre preto"
    - "D-04: slots de foto têm placeholder monocromático intencional via RamonPhoto src? — troca placeholder→foto trivial"
    - "D-05: RamonPhoto renderiza foto full-bleed P&B alto contraste (grayscale contrast-125)"
    - "D-06: scrim (overlay/gradiente) obrigatório aplicado sobre a foto pelo RamonPhoto para legibilidade AA"
    - "D-07: existe a pasta-convenção public/ramon/ documentada para o usuário soltar fotos"
    - "D-08: ParallaxImage faz parallax leve (só translate), sem pin/scrub coreografado"
    - "D-09: microinterações mínimas — hover do CTA via CSS e AnimatedCounter reusado"
    - "D-10: mapa de libs respeitado — island de motion NÃO importa three.js nesta fase"
    - "Animação GSAP (parallax) vive isolada em components/motion/ como client island"
  artifacts:
    - path: "site/lib/usePrefersReducedMotion.ts"
      provides: "Hook central de detecção de reduced-motion"
      contains: "matchMedia"
    - path: "site/components/motion/ParallaxImage.tsx"
      provides: "Parallax leve de foto via GSAP useGSAP + gsap.matchMedia"
      contains: "use client"
    - path: "site/components/RamonPhoto.tsx"
      provides: "next/image full-bleed P&B + scrim com fallback placeholder intencional"
      contains: "next/image"
    - path: "site/app/globals.css"
      provides: "Tokens reconciliados + tokens sobre-branco + scrims"
      contains: "--color-muted-on-light"
  key_links:
    - from: "site/components/AnimatedCounter.tsx"
      to: "site/lib/usePrefersReducedMotion.ts"
      via: "import + gate"
      pattern: "usePrefersReducedMotion"
    - from: "site/components/motion/ParallaxImage.tsx"
      to: "@gsap/react"
      via: "useGSAP"
      pattern: "useGSAP"
---

<objective>
Fundar a camada de design/animação que todas as seções redesenhadas vão consumir, num slice vertical fino que já muda o site visivelmente e mantém o build verde. Reconcilia os tokens de cor divergentes, adiciona os tokens de contraste-sobre-branco e os scrims, instala `@gsap/react`, cria o hook central de reduced-motion, aplica gates JS de reduced-motion nas ilhas de animação legadas (Reveal, AnimatedCounter, CTAButton), e cria os dois componentes novos que o redesign foto-conduzido exige: `RamonPhoto` (foto P&B + scrim com fallback de placeholder intencional) e `ParallaxImage` (parallax leve GSAP isolado como client island).

Após este plano: a home existente renderiza sobre os tokens reconciliados, com reduced-motion realmente respeitado (animação não dispara, não só acelera), o componente de foto pronto para receber arquivos em `public/ramon/`, e o budget de bundle medível.

Purpose: Esta é a fundação não-retrofitável (DSGN-01..05) exigida antes de qualquer redesign de seção. Os Planos 02 e 03 dependem inteiramente dela.
Output: globals.css reconciliado, hook + ParallaxImage + RamonPhoto novos, gates em Reveal/AnimatedCounter/CTAButton, `public/ramon/` convencionada, `@gsap/react` instalado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-editorial-funda-o/01-CONTEXT.md
@.planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md
@.planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md
@.planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md

<interfaces>
<!-- Contratos que o executor deve preservar exatamente. Extraídos do código atual. -->

Reveal (site/components/Reveal.tsx) — preservar assinatura idêntica:
  export function Reveal({ children, delay = 0, className }: { children: ReactNode; delay?: number; className?: string })

AnimatedCounter (site/components/AnimatedCounter.tsx) — preservar assinatura:
  export function AnimatedCounter({ to, duration = 1.6, prefix = "", suffix = "" }: { to: number; duration?: number; prefix?: string; suffix?: string })

CTAButton (site/components/ui/CTAButton.tsx) — preservar assinatura:
  export function CTAButton({ href, children, variant = "primary", className }: { href: string; children: ReactNode; variant?: "primary" | "outline"; className?: string })
  styles atuais: primary "bg-fg text-bg hover:bg-muted", outline "border border-fg/40 ...". Classe base usa cn() de @/lib/utils.

cn (site/lib/utils.ts): concat simples — mantê-lo (clsx+tailwind-merge é OPCIONAL, não introduzir salvo conflito real de classes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconciliar tokens, scrims e foco em globals.css + instalar @gsap/react + pasta public/ramon</name>
  <files>site/app/globals.css, site/package.json, site/package-lock.json, site/public/ramon/README.md</files>
  <read_first>
    - site/app/globals.css (estado atual: --color-bg #000000, --color-fg #ffffff, --color-surface #0c0c0c, --color-line #1f1f1f; bloco @media reduced-motion linhas ~44-53; :focus-visible outline branco)
    - .planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md §Design Tokens (tabela de cores + tokens de texto + scrims)
    - .planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md §Code Examples "Tokens reconciliados em Tailwind v4" + §Runtime State Inventory item 1 (discrepância de tokens) + §Package Legitimacy Audit
    - .planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md §Shared Patterns "Token system"
  </read_first>
  <action>
    No bloco `@theme` de site/app/globals.css, ESTENDER em lugar (não renomear nomes existentes): trocar `--color-bg` de `#000000` para `#0a0a0a`; `--color-fg` de `#ffffff` para `#ededed`; `--color-surface` de `#0c0c0c` para `#141414`; `--color-line` de `#1f1f1f` para `#262626`. MANTER `--color-muted: #7f7f7f` (passa AA sobre preto, D-03 — não alterar). Adicionar dois tokens NOVOS: `--color-fg-on-light: #0a0a0a` (texto primário sobre branco) e `--color-muted-on-light: #595959` (texto secundário sobre branco, ≥4.5:1 — NUNCA usar #7f7f7f sobre branco, que dá ~3.5:1 e falha). No bloco `:root` (que já tem `color-scheme: dark`), adicionar dois custom properties de scrim: `--scrim-hero: linear-gradient(180deg, rgba(0,0,0,0.35) 0%, rgba(0,0,0,0.75) 100%)` e `--scrim-portrait: linear-gradient(180deg, rgba(0,0,0,0) 0%, rgba(0,0,0,0.6) 100%)`. Manter os blocos `body`, `::selection`, `:focus-visible` e `@media (prefers-reduced-motion: reduce)` intactos (este último é a rede de segurança que permanece, DSGN-03). Os valores hex de `::selection` (#ffffff/#000000) e do `:focus-visible` (outline branco) podem permanecer como estão.
    Instalar a única dependência nova da fase, a partir de dentro de `site/`: `@gsap/react@2.1.2` (peer `gsap ^3.12.5` satisfeito por gsap@3.15.0 já instalado). É o pacote oficial GreenSock prescrito na doc oficial; ver checkpoint de legitimidade na Task 2. NÃO reinstalar gsap/animejs/framer-motion/three (já instalados). NÃO importar three nesta fase.
    Criar a pasta-convenção de assets `site/public/ramon/` com um arquivo `README.md` documentando: a pasta recebe as fotos P&B do Ramon (D-07); arquivos esperados `hero.jpg` (foto dominante do Hero, full-bleed) e `retrato.jpg` (retrato vertical 4/5 do SobreRamon); enquanto ausentes, o componente RamonPhoto renderiza placeholder monocromático intencional; basta soltar os arquivos com esses nomes para a troca ser trivial (D-04). Não comitar binários — só o README.
  </action>
  <verify>
    <automated>cd "site" && grep -v '^#' app/globals.css | grep -c 'color-muted-on-light' | grep -qv '^0$' && grep -v '^#' app/globals.css | grep -q 'scrim-hero' && node -e "const p=require('./package.json'); process.exit(p.dependencies['@gsap/react']?0:1)" && test -f public/ramon/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/app/globals.css` @theme contém `--color-bg: #0a0a0a`, `--color-fg: #ededed`, `--color-surface: #141414`, `--color-line: #262626`, `--color-fg-on-light: #0a0a0a`, `--color-muted-on-light: #595959`, e mantém `--color-muted: #7f7f7f`
    - `site/app/globals.css` :root contém `--scrim-hero` e `--scrim-portrait`
    - `site/app/globals.css` ainda contém o bloco `@media (prefers-reduced-motion: reduce)` (rede de segurança preservada)
    - `site/package.json` dependencies contém `@gsap/react`; `cd site && npm ls @gsap/react` resolve sem erro
    - `site/public/ramon/README.md` existe e documenta os nomes `hero.jpg` e `retrato.jpg`
    - `site/next.config.ts` NÃO ganhou `images.remotePatterns` (fotos servidas same-origin de public/)
  </acceptance_criteria>
  <done>Tokens reconciliados + tokens sobre-branco + scrims presentes; @gsap/react instalado; public/ramon/ convencionada e documentada; build/lint ainda passa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verificação de legitimidade de @gsap/react (slopcheck indisponível)</name>
  <action>Pausar para confirmação humana da legitimidade do pacote @gsap/react antes de qualquer uso de useGSAP (Task 4). Não há ação de código: o executor apresenta o checkpoint e aguarda o sinal de retomada.</action>
  <what-built>Instalação de `@gsap/react@2.1.2` adicionada ao package.json na Task 1.</what-built>
  <how-to-verify>
    slopcheck não está disponível no ambiente, então sob a regra de degradação graciosa este pacote é tratado como `[ASSUMED]` e exige confirmação humana antes de prosseguir (T-01-SC). Confirme em https://www.npmjs.com/package/@gsap/react que: (1) o publisher é a org oficial GreenSock (mesma org de `gsap`), (2) o repositório aponta para github.com/greensock/react, (3) o pacote tem volume de download consistente (classe ~600k/semana). A pesquisa já verificou peer-deps contra o lockfile. Se algo destoar, NÃO prossiga e reporte.
  </how-to-verify>
  <resume-signal>Digite "approved" para confirmar a legitimidade e liberar o uso de useGSAP, ou descreva o problema encontrado.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Hook usePrefersReducedMotion + gates JS em Reveal, AnimatedCounter e CTAButton</name>
  <files>site/lib/usePrefersReducedMotion.ts, site/components/Reveal.tsx, site/components/AnimatedCounter.tsx, site/components/ui/CTAButton.tsx</files>
  <read_first>
    - site/components/Reveal.tsx (Framer motion.div, variants fade+y:24, once:true; HOJE sem gate JS — comentário alega "via CSS global", insuficiente)
    - site/components/AnimatedCounter.tsx (useEffect + rAF tick que sempre dispara em inView)
    - site/components/ui/CTAButton.tsx (link, cn(), transition-colors, sem hover scale nem focus-visible explícito no elemento)
    - .planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md §Code Examples (hook usePrefersReducedMotion verbatim linhas ~332-348; gate Framer Reveal linhas ~352-360; gate AnimatedCounter linhas ~363-370; microinteração anime.js + nota CSS alternativa linhas ~373-382)
    - .planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md §Reduced-motion gates per lib + entradas das três modificações (Reveal/AnimatedCounter/CTAButton)
    - .planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md §States & Interactions + §Accessibility Contract (focus ring monocromático, hover scale 1.04)
  </read_first>
  <action>
    Criar `site/lib/usePrefersReducedMotion.ts`: client component (`"use client"`), hook que retorna `boolean`, usa `useState(false)` + `useEffect` que assina `window.matchMedia("(prefers-reduced-motion: reduce)")`, seta o estado inicial de `mq.matches`, registra `mq.addEventListener("change", ...)` e limpa com `removeEventListener` no cleanup. É o ponto único de centralização (DSGN-03, RESEARCH §Don't Hand-Roll) — implementar conforme o exemplo da RESEARCH.
    Em `site/components/Reveal.tsx`: importar `useReducedMotion` de framer-motion; se `reduce` for true, retornar early `<div className={className}>{children}</div>` (estático e completo — não animar rápido); caso contrário, o `motion.div` atual com variants/transition preservados. Manter a assinatura de props idêntica.
    Em `site/components/AnimatedCounter.tsx`: consumir `usePrefersReducedMotion()`; DENTRO do useEffect, antes do loop rAF, `if (reduce) { setValue(to); return; }` (mostra o valor final imediato); adicionar `reduce` ao array de dependências do useEffect. Preservar a assinatura.
    Em `site/components/ui/CTAButton.tsx` (mantém-se Server Component — sem "use client"): adicionar à classe base, via `cn()`, a microinteração de hover por CSS puro `hover:scale-[1.04]` + `transition-transform` (decisão do planner conforme RESEARCH A2: CSS satisfaz "microinteração mínima" de D-09 e auto-respeita reduced-motion pelo @media global, evitando uma island extra e mantendo o CTAButton como RSC). Adicionar também `focus-visible:` ring monocromático visível no próprio elemento — branco sobre fundo escuro (ex.: `focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-fg`), contextualizável por className quando usado sobre bloco branco. Manter `bg-fg text-bg hover:bg-muted` (primary) e `hover:bg-neutral-200` não é necessário — manter a paleta por token. Nota de copy: o label "Quero minha consultoria" é reconciliado nos Planos 02/03 (uso em page.tsx/seções), não aqui.
    Decisão registrada (D-09 / RESEARCH A2): hover do CTA via CSS `transition-transform`, NÃO anime.js — não criar `CtaMicroFx.tsx`. anime.js permanece instalada e disponível mas não usada nesta fase; o mapa de libs (DSGN-02 / D-10) segue intacto como contrato.
  </action>
  <verify>
    <automated>cd "site" && grep -q "matchMedia" lib/usePrefersReducedMotion.ts && grep -q "useReducedMotion" components/Reveal.tsx && grep -q "usePrefersReducedMotion" components/AnimatedCounter.tsx && grep -q "hover:scale" components/ui/CTAButton.tsx && npm run lint && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/lib/usePrefersReducedMotion.ts` existe, é `"use client"`, retorna boolean, usa `window.matchMedia("(prefers-reduced-motion: reduce)")` com addEventListener/removeEventListener
    - `site/components/Reveal.tsx` importa `useReducedMotion` de framer-motion e tem branch estático quando reduce=true
    - `site/components/AnimatedCounter.tsx` consome `usePrefersReducedMotion` e faz `setValue(to)` + return antes do rAF quando reduce=true; `reduce` está nas deps do useEffect
    - `site/components/ui/CTAButton.tsx` contém `hover:scale-[1.04]` e um `focus-visible:` ring; NÃO contém `"use client"`
    - Não existe `site/components/motion/CtaMicroFx.tsx` (decisão: CSS, não anime.js)
    - `npm run lint` e `npx tsc --noEmit` passam
    - Comportamento: com DevTools > Rendering > Emulate `prefers-reduced-motion: reduce`, recarregar a home → Reveal aparece estático e completo, AnimatedCounter mostra valor final sem contar, hover do CTA não escala
  </acceptance_criteria>
  <done>Hook central existe; Reveal/AnimatedCounter gateados em JS (animação não dispara sob reduce); CTAButton com hover scale CSS + focus ring monocromático; lint+typecheck verdes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: RamonPhoto (next/image + scrim + placeholder) e ParallaxImage (GSAP island)</name>
  <files>site/components/RamonPhoto.tsx, site/components/motion/ParallaxImage.tsx</files>
  <read_first>
    - site/components/sections/Hero.tsx (placeholder atual: radial-gradient aria-hidden + watermark DINO — o slot a ser substituído nos planos seguintes)
    - site/components/sections/SobreRamon.tsx (placeholder de retrato: role="img" + aria-label + aspect-[4/5] + gradiente + watermark)
    - site/components/Reveal.tsx (esqueleto de client island "use client" para espelhar em ParallaxImage)
    - .planning/phases/01-landing-editorial-funda-o/01-RESEARCH.md §Pattern 1 (useGSAP + gsap.matchMedia, verbatim linhas ~202-227), §Pattern 2 (next/image fill priority sizes grayscale contrast-125 + scrim, linhas ~234-247), §Pattern 3 (placeholder trivialmente substituível via prop src?, linhas ~250-253), §Pitfall 6 (CLS/LCP — só transform/opacity, priority no Hero)
    - .planning/phases/01-landing-editorial-funda-o/01-PATTERNS.md entradas RamonPhoto e ParallaxImage
    - .planning/phases/01-landing-editorial-funda-o/01-UI-SPEC.md §Component Inventory (RamonPhoto, ParallaxImage) + §Accessibility Contract (alt on-brand, aria-hidden no overlay, priority no LCP)
  </read_first>
  <action>
    Criar `site/components/RamonPhoto.tsx` (componente presentacional, SEM "use client" — usa next/image que é server-friendly; é um componente de renderização, a parallax fica fora dele): props `{ src?: string; alt: string; priority?: boolean; scrim: "hero" | "portrait"; className?: string }`. Se `src` presente (D-05): renderizar `<Image>` de `next/image` com `fill`, `sizes="100vw"` (Hero) ou apropriado, `priority` quando a prop for true (LCP do Hero) senão `loading` default lazy, e `className="object-cover object-center grayscale contrast-125"` (P&B alto contraste — caso a origem seja colorida). Sobre a imagem, um overlay `aria-hidden` com `style={{ background: scrim === "hero" ? "var(--scrim-hero)" : "var(--scrim-portrait)" }}` (scrim obrigatório para legibilidade AA — D-06; usar os tokens CSS, NUNCA hex inline — RESEARCH Anti-Pattern). Se `src` AUSENTE (D-04 — placeholder intencional, troca trivial): renderizar placeholder monocromático INTENCIONAL (não gradiente colorido, não estado de erro) — fundo `bg-surface` + borda `border border-line`, com rótulo Anton uppercase "Foto do Ramon" centralizado em `text-muted`, `role="img"` + `aria-label` descritivo. O contêiner externo deve aceitar `className` para o consumidor controlar dimensões (full-bleed no Hero, aspect-[4/5] no SobreRamon) e ser `relative overflow-hidden` para conter o `fill`. Nota: scrim é piso de partida; o executor mede o contraste do texto sobreposto na região mais clara e sobe a opacidade do token se necessário (≥4.5:1, critério de aceite 2 — D-03) — ajuste documentado no SUMMARY.
    Criar `site/components/motion/ParallaxImage.tsx` (client island, `"use client"` — DSGN-05): wrapper que recebe `children` (a foto) e aplica parallax LEVE via GSAP. Registrar `gsap.registerPlugin(ScrollTrigger, useGSAP)`; usar `useGSAP(() => { const mm = gsap.matchMedia(); mm.add("(prefers-reduced-motion: no-preference)", () => { gsap.to(... translateY leve ..., { scrollTrigger: { trigger: scope, scrub: true } }) }) }, { scope })`. CONSTRAINT (D-08): apenas translate vertical leve, SEM pin, SEM scrub coreografado rico — só um deslocamento sutil. Animar somente `transform`/`y` (nunca height/top — RESEARCH Pitfall 6). Sob reduced-motion a timeline NÃO é criada (gsap.matchMedia "no-preference"), foto fica estática. Em telas pequenas, degradar para movimento mínimo ou desligado (UI-SPEC §Responsive) — pode usar a query de matchMedia ou amplitude reduzida; não pode causar jank/CLS. O island NÃO importa three (D-10 — three.js ociosa nesta fase). As seções (Planos 02/03) importarão ParallaxImage e RamonPhoto; nenhuma seção recebe "use client".
  </action>
  <verify>
    <automated>cd "site" && grep -q "next/image" components/RamonPhoto.tsx && grep -q "scrim-hero" components/RamonPhoto.tsx && head -1 components/motion/ParallaxImage.tsx | grep -q "use client" && grep -q "useGSAP" components/motion/ParallaxImage.tsx && grep -q "matchMedia" components/motion/ParallaxImage.tsx && npm run lint && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/RamonPhoto.tsx` usa `next/image` com `fill`, aplica `grayscale contrast-125`, overlay de scrim via `var(--scrim-hero)`/`var(--scrim-portrait)` (sem hex inline), e renderiza placeholder Anton "Foto do Ramon" com `role="img"`+`aria-label` quando `src` ausente
    - `site/components/RamonPhoto.tsx` NÃO contém `"use client"` (componente de render)
    - `site/components/motion/ParallaxImage.tsx` é `"use client"`, usa `useGSAP` de @gsap/react e `gsap.matchMedia("(prefers-reduced-motion: no-preference)")`, anima só transform, sem `pin:` 
    - Nenhum dos dois importa `three`
    - `npm run lint` e `npx tsc --noEmit` passam
  </acceptance_criteria>
  <done>RamonPhoto pronto (foto+scrim ou placeholder intencional, troca trivial); ParallaxImage isolado em components/motion/ com gate GSAP; lint+typecheck verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → CDN/browser | Conteúdo estático monocromático renderizado; sem input de usuário, sem auth, sem datastore |
| npm registry → build | Instalação de `@gsap/react` cruza a fronteira de supply-chain |

Esta fase é um redesign de landing estática frontend-only: sem superfície de autenticação, sem entrada de usuário, sem persistência de dados. A superfície de ameaça é mínima e majoritariamente N/A.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install `@gsap/react` | mitigate | Task 2 = checkpoint blocking-human; slopcheck indisponível → tratado [ASSUMED]; verificar publisher/repo/downloads em npmjs.com; peer-deps já validados contra lockfile |
| T-01-IMG | Tampering | next/image source | mitigate | Fotos servidas same-origin de `public/ramon/`; NÃO abrir `images.remotePatterns` no next.config.ts (RESEARCH §Security V14) |
| T-01-TRACK | Info Disclosure | TrackingScripts (LGPD) | accept | Fora de escopo (Fase 3); este plano NÃO toca `TrackingScripts.tsx` nem acopla script de terceiro |
| T-01-XSS | Tampering | conteúdo renderizado | accept | Todo texto/alt é estático no código; sem `dangerouslySetInnerHTML`, sem input de usuário — risco N/A |
</threat_model>

<verification>
- `cd site && npm run lint && npx tsc --noEmit` verde após cada task
- `cd site && npm run build` verde ao fim do plano; ler "First Load JS" da rota `/` e registrar no SUMMARY (orçamento <200KB — DSGN-02; baseline desta fase, sem foto/parallax ainda nas seções)
- DevTools > Rendering > Emulate `prefers-reduced-motion: reduce`: Reveal estático, AnimatedCounter mostra valor final, hover de CTA não escala
- `grep -rL "use client" components/sections app/page.tsx` permanece limpo (nenhuma seção/página virou client) — DSGN-05
</verification>

<success_criteria>
- Tokens reconciliados (#0a0a0a/#ededed/#141414/#262626) + `--color-fg-on-light`/`--color-muted-on-light` + scrims presentes em globals.css
- `@gsap/react` instalado e aprovado no checkpoint de legitimidade
- `usePrefersReducedMotion` central existe; Reveal/AnimatedCounter não disparam animação sob reduced-motion
- CTAButton com hover scale CSS + focus ring monocromático, permanece RSC
- RamonPhoto (foto+scrim/placeholder) e ParallaxImage (GSAP island) criados; nenhuma seção marcada "use client"
- `public/ramon/` documentada para o usuário soltar fotos
- build + lint + typecheck verdes
</success_criteria>

<output>
Create `.planning/phases/01-landing-editorial-funda-o/01-01-SUMMARY.md` when done. Registrar no SUMMARY: o First Load JS medido da rota `/`, qualquer ajuste de opacidade de scrim feito para atingir AA, e a confirmação do contraste de `#7f7f7f` sobre `#0a0a0a` (~4.5:1, piso — RESEARCH A4).
</output>
</content>
</invoke>
