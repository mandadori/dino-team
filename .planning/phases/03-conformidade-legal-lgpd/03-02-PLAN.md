---
phase: 03-conformidade-legal-lgpd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/app/privacidade/page.tsx
  - site/app/termos/page.tsx
  - site/app/page.tsx
autonomous: true
requirements: [LEGAL-01, LEGAL-02]
user_setup: []

must_haves:
  truths:
    - "A visitor navigates to /privacidade and reads a complete Política de Privacidade in the brand editorial style"
    - "A visitor navigates to /termos and reads complete Termos de Uso in the same style"
    - "Both legal pages sit under the same fixed header and footer as the landing"
    - "The home footer contains links to /privacidade and /termos"
    - "Company identifiers in the legal copy are bracketed placeholders the user can swap without touching code"
  artifacts:
    - path: "site/app/privacidade/page.tsx"
      provides: "RSC Política de Privacidade page, brand editorial layout"
      contains: "POLÍTICA DE PRIVACIDADE"
      min_lines: 60
    - path: "site/app/termos/page.tsx"
      provides: "RSC Termos de Uso page, brand editorial layout"
      contains: "TERMOS DE USO"
      min_lines: 60
    - path: "site/app/page.tsx"
      provides: "Footer with legal links"
      contains: "/termos"
  key_links:
    - from: "site/app/page.tsx footer"
      to: "/privacidade and /termos"
      via: "next/link"
      pattern: "/privacidade"
    - from: "site/app/privacidade/page.tsx"
      to: "design tokens"
      via: "bg-bg / text-fg / font-display"
      pattern: "max-w-3xl"
---

<objective>
Ship the legal-pages vertical slice (LEGAL-01, LEGAL-02): a visitor can navigate to /privacidade and /termos — complete, on-brand, readable legal documents — and reach them from the home footer.

Purpose: The cookie banner (Plan 01) links to /privacidade, and LGPD requires a real privacy policy and terms of use to back data collection. These pages must exist and be reachable.

Output: `app/privacidade/page.tsx`, `app/termos/page.tsx` (RSC pages inheriting the landing's header/footer shell), and legal links added to the existing home footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conformidade-legal-lgpd/03-CONTEXT.md
@.planning/phases/03-conformidade-legal-lgpd/03-UI-SPEC.md
@.planning/phases/03-conformidade-legal-lgpd/03-PATTERNS.md
@brand/tom-de-voz.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

Home header to replicate (app/page.tsx lines 17-26):
  header className="fixed inset-x-0 top-0 z-50 border-b border-line/60 bg-bg/80 backdrop-blur"
    div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4"
      span className="font-display text-2xl uppercase tracking-wide" → "Dino Team"
      CTAButton href={WHATSAPP_URL} className="px-5 py-2.5 text-xs" → "Quero minha direção"

Home footer to replicate + extend (app/page.tsx lines 41-51):
  footer className="border-t border-line px-6 py-12"
    div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-4 text-center sm:flex-row sm:text-left"
      span (display, "Dino Team") + p (text-sm text-muted, consultoria tagline)

CTAButton import: import { CTAButton } from "@/components/ui/CTAButton"
WHATSAPP_URL import: import { WHATSAPP_URL } from "@/lib/site"
Link import: import Link from "next/link"

Tailwind tokens (globals.css @theme): bg-bg (#0a0a0a), text-fg (#ededed), text-muted (#7f7f7f), border-line (#262626). font-display (Anton, always uppercase), font-body (Montserrat).

Note: header + footer currently live ONLY in app/page.tsx, NOT in the root layout. Legal pages must include their own header + footer markup (or the executor may extract a shared SiteShell component used by all three).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /privacidade page (RSC, brand editorial)</name>
  <files>site/app/privacidade/page.tsx</files>
  <read_first>
    - site/app/page.tsx (copy the exact header markup lines 17-26 and footer markup lines 41-51)
    - site/app/admin/dashboard/page.tsx (RSC page heading-hierarchy analog)
    - site/components/sections/FAQ.tsx (max-w-3xl prose container reference)
    - site/components/ui/CTAButton.tsx (header CTA usage)
    - brand/tom-de-voz.md (sober tone for the legal prose)
  </read_first>
  <action>
    Create `site/app/privacidade/page.tsx` as a pure RSC (NO `"use client"` — D-02, static page). It reuses the landing's visual shell: render the fixed header (copied from `app/page.tsx` lines 17-26: `Dino Team` wordmark + `CTAButton` to `WHATSAPP_URL`) and the footer (copied from `app/page.tsx` lines 41-51) so the page sits under the same fixed header rhythm. Import `CTAButton` from `@/components/ui/CTAButton`, `WHATSAPP_URL` from `@/lib/site`, `Link` from `next/link`.

    Main content container: `main` with top padding that clears the fixed header — classes `px-6 py-24 sm:py-32 pt-[calc(theme(spacing.24)+4rem)] sm:pt-[calc(theme(spacing.32)+4rem)]`, inner `div className="mx-auto max-w-3xl"`.
    - H1: `font-display text-4xl uppercase leading-[1.1] sm:text-5xl text-fg` → "POLÍTICA DE PRIVACIDADE".
    - Meta line: `mt-4 font-body text-sm text-muted` → "Última atualização: [DATA]" (bracketed placeholder).
    - Sections: each an H2 `font-display text-2xl uppercase leading-[1.2]` followed by Montserrat body `font-body text-base leading-relaxed text-fg`, sections separated by `space-y-8` / `mt-16` rhythm. Optional sub-headings use Montserrat 600 `text-base uppercase tracking-wide` (NOT a new size step — UI-SPEC typography).

    Generate REAL, complete LGPD-grade privacy-policy prose in Portuguese (D-01 — full draft, not a stub), covering at minimum: identificação do controlador, dados pessoais coletados (incluindo cookies de analytics GA4/Meta/Clarity), finalidade do tratamento, base legal (consentimento), compartilhamento com terceiros (provedores de analytics), direitos do titular sob a LGPD, retenção, e contato do encarregado. Mark company identifiers as literal bracketed tokens: `[RAZÃO SOCIAL]`, `[CNPJ]`, `[E-MAIL DO ENCARREGADO DE DADOS]` so the user swaps them without touching code. Tone: sober, second person, per `brand/tom-de-voz.md`; no marketing fluff, no clichés. Single H1, ordered H2s (semantic hierarchy).

    Add a Next.js `metadata` export (title + description) for the page.
  </action>
  <verify>
    <automated>cd "site" && npx tsc --noEmit 2>&1 | tail -5; echo "no test runner — gate via tsc/build + grep"; grep -c "use client" app/privacidade/page.tsx; grep -c "POLÍTICA DE PRIVACIDADE" app/privacidade/page.tsx; grep -c "max-w-3xl" app/privacidade/page.tsx; grep -c "RAZÃO SOCIAL\|CNPJ\|ENCARREGADO" app/privacidade/page.tsx; grep -c "WHATSAPP_URL" app/privacidade/page.tsx</automated>
  </verify>
  <done>
    `/privacidade` renders an RSC page with the landing header + footer, brand typography/tokens, complete LGPD privacy-policy prose, and bracketed identifier placeholders. `tsc --noEmit` passes.
  </done>
  <acceptance_criteria>
    - app/privacidade/page.tsx does NOT contain `"use client"` (0 occurrences)
    - app/privacidade/page.tsx contains the H1 text `POLÍTICA DE PRIVACIDADE`
    - app/privacidade/page.tsx contains `max-w-3xl` and replicates the fixed header (`fixed inset-x-0 top-0`) and footer (`border-t border-line px-6 py-12`)
    - app/privacidade/page.tsx contains `[RAZÃO SOCIAL]`, `[CNPJ]`, and `[E-MAIL DO ENCARREGADO DE DADOS]`
    - The privacy prose references analytics cookies / GA4 / Meta / Clarity and LGPD data-subject rights
    - `npx tsc --noEmit` produces no errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: /termos page + footer legal links</name>
  <files>site/app/termos/page.tsx, site/app/page.tsx</files>
  <read_first>
    - site/app/privacidade/page.tsx (the page created in Task 1 — copy its structure/shell exactly; only H1 + prose differ)
    - site/app/page.tsx (footer block lines 41-51 — add legal links inside the existing flex div)
    - site/app/admin/layout.tsx (nav link styling reference: text-muted hover:text-fg)
    - brand/tom-de-voz.md (sober tone for the terms prose)
  </read_first>
  <action>
    Create `site/app/termos/page.tsx` as a pure RSC, structurally identical to `app/privacidade/page.tsx` (same header, footer, container, typography, spacing — copy every pattern). Only the H1 text and prose differ: H1 "TERMOS DE USO". Generate REAL, complete terms-of-use prose in Portuguese (D-01) covering: aceitação dos termos, descrição do serviço (consultoria de treino/dieta + conteúdo do site, sem promessa de resultado — respeitar tabu de marca), propriedade intelectual, conduta do usuário, limitação de responsabilidade, links externos (WhatsApp/terceiros), alterações dos termos, foro/legislação aplicável (Brasil), and contato. Use the same bracketed identifier placeholders (`[RAZÃO SOCIAL]`, `[CNPJ]`, `[E-MAIL DO ENCARREGADO DE DADOS]`) where company identity is referenced. Meta line "Última atualização: [DATA]". Add a Next.js `metadata` export. Do NOT promise outcomes/timelines (brand tabu, REQUIREMENTS §Out of Scope).

    Modify `site/app/page.tsx` footer (lines 41-51): add a `nav` inside the existing footer flex `div` (after the tagline `p`), containing two `next/link` elements — to `/privacidade` (text "Política de Privacidade") and `/termos` (text "Termos de Uso"). Classes: `flex gap-5 font-body text-sm text-muted` on the nav; each link `hover:text-fg` plus the focus-ring token `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-fg focus-visible:ring-offset-bg`. Keep the outer `footer` structure and existing tagline intact (D-04). `import Link from "next/link"` (page.tsx is a Next page; add the import if absent).
  </action>
  <verify>
    <automated>cd "site" && npx tsc --noEmit 2>&1 | tail -5; npx next build 2>&1 | tail -15; grep -c "use client" app/termos/page.tsx; grep -c "TERMOS DE USO" app/termos/page.tsx; grep -c "max-w-3xl" app/termos/page.tsx; grep -c "/privacidade" app/page.tsx; grep -c "/termos" app/page.tsx; grep -c "next/link" app/page.tsx</automated>
  </verify>
  <done>
    `/termos` renders an RSC page mirroring `/privacidade` with complete terms-of-use prose and no outcome promises. The home footer links to both `/privacidade` and `/termos` with brand link styling. `next build` succeeds (both routes build statically).
  </done>
  <acceptance_criteria>
    - app/termos/page.tsx does NOT contain `"use client"` (0 occurrences)
    - app/termos/page.tsx contains the H1 text `TERMOS DE USO` and `max-w-3xl`
    - app/termos/page.tsx contains the bracketed identifier placeholders
    - app/termos/page.tsx prose does NOT promise a result/timeline (no "garantimos resultado", no fixed-deadline promise)
    - app/page.tsx footer contains `next/link` import, a link with `href="/privacidade"` and a link with `href="/termos"`, styled `text-muted hover:text-fg`
    - app/page.tsx footer retains the existing "Dino Team" wordmark and consultoria tagline
    - `npx next build` completes and emits both `/privacidade` and `/termos` routes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → static legal pages | Read-only RSC pages, no user input, no data submission this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | placeholder identifiers shipped to prod | accept | `[RAZÃO SOCIAL]`/`[CNPJ]`/`[E-MAIL...]` are intentional placeholders (D-01); user swaps before going live. No secret leaked. |
| T-03-05 | Tampering | external links in terms (WhatsApp/third-party) | mitigate | External anchors use `rel="noopener noreferrer"` where `target="_blank"` is used (inherits CTAButton convention). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase. Audit table not required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npx next build` emits `/privacidade` and `/termos` as static routes and rebuilds `/`.
- Source assertions in each task's `<acceptance_criteria>` hold.
- Visual conformance to 03-UI-SPEC (tokens, max-w-3xl, Anton/Montserrat) is checked by the ui-checker downstream.
</verification>

<success_criteria>
- A visitor reaches navigable, on-brand /privacidade and /termos pages (LEGAL-01, LEGAL-02 — success criterion 1).
- Both pages carry complete legal prose with bracketed identifier placeholders (D-01).
- The home footer links to both pages (D-04).
- Build and type-check pass.
</success_criteria>

<output>
Create `.planning/phases/03-conformidade-legal-lgpd/03-02-SUMMARY.md` when done.
</output>
