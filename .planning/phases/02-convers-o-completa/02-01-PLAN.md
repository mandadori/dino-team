---
phase: 02-convers-o-completa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/lib/site.ts
  - site/components/sections/Planos.tsx
  - site/components/sections/Resultados.tsx
  - site/app/page.tsx
autonomous: true
requirements: [CONV-01, CONF-01]
must_haves:
  truths:
    - "Um visitante vê a seção Planos com 2 cards, o preço visível em cada card e a lista de inclusos por plano"
    - "Cada card de Plano tem um CTA WhatsApp que abre um deeplink personalizado (wa.me/NUMBER?text=Quero+o+Plano+X)"
    - "Os cards de Plano são monocromáticos, distintos por tonalidade (bg vs surface), sem badge popular nem cor de destaque"
    - "O bloco tracejado de placeholder de depoimentos em Resultados.tsx não existe mais"
    - "O build, lint e typecheck passam com os placeholders de conteúdo em lib/site.ts"
  artifacts:
    - path: "site/lib/site.ts"
      provides: "Constantes PLANS, TESTIMONIALS, COMMUNITY_BENEFITS, COMMUNITY_WHATSAPP_URL"
      contains: "export const PLANS"
    - path: "site/components/sections/Planos.tsx"
      provides: "Seção Planos RSC com grade de 2 cards"
      contains: "PLANS.map"
      min_lines: 25
    - path: "site/app/page.tsx"
      provides: "Registro da seção Planos na ordem D-01"
      contains: "<Planos />"
  key_links:
    - from: "site/components/sections/Planos.tsx"
      to: "site/lib/site.ts"
      via: "import { PLANS }"
      pattern: "import.*PLANS.*from.*@/lib/site"
    - from: "site/components/sections/Planos.tsx"
      to: "CTAButton"
      via: "per-plan deeplink href={p.whatsappUrl}"
      pattern: "href=\\{p\\.whatsappUrl\\}"
---

## Phase Goal

**As a** visitante interessado em consultoria, **I want to** ver o preço dos planos e chegar ao WhatsApp já dizendo qual plano quero, **so that** eu fecho a decisão de contratar sem atrito.

> Slice deste plano (wave 1): fundação de dados de conversão + a seção Planos completa ponta a ponta — após este plano o visitante já vê preço, inclusos e um CTA deeplink por plano na página real. Depoimentos e Comunidade vêm no plano 02-02.

<objective>
Construir a fundação de dados de conversão e o slice vertical de Planos: o visitante passa a ver preço, inclusos e um CTA WhatsApp deeplink por plano — o argumento de conversão se fecha na página.

Purpose: CONV-01 (Planos com preço + CTA por plano) + base de CONF-01 (deeplinks de WhatsApp por plano em lib/site.ts). Esta é a fatia mais pesada da fase — entrega a seção que fecha o funil.

Output: `lib/site.ts` com 4 novas constantes (todas com placeholders seguros), `Planos.tsx` (seção RSC), Planos registrado em `page.tsx` na ordem D-01, e o bloco tracejado de placeholder removido de `Resultados.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-convers-o-completa/02-CONTEXT.md
@.planning/phases/02-convers-o-completa/02-UI-SPEC.md
@.planning/phases/02-convers-o-completa/02-PATTERNS.md
@brand/brand-book.md
@brand/tom-de-voz.md

<interfaces>
<!-- Contratos existentes que o executor consome. Use diretamente — sem explorar o codebase. -->

From site/lib/site.ts (existing patterns to mirror):
```typescript
export const WHATSAPP_URL =
  process.env.NEXT_PUBLIC_WHATSAPP_URL ||
  "https://wa.me/0000000000?text=" + encodeURIComponent("Quero minha consultoria Dino Team");

export const STATS: ReadonlyArray<{ value: number; prefix?: string; suffix: string; label: string }> = [ ... ];
export const TIMELINE: ReadonlyArray<{ marco: string; texto: string }> = [ ... ];
```

From site/components/ui/CTAButton.tsx:
```typescript
// Renderiza <a target="_blank" rel="noopener noreferrer"> com focus-ring e hover:scale reduced-motion-safe.
export function CTAButton(props: { href: string; children: ReactNode; variant?: "primary" | "outline"; className?: string }): JSX.Element;
```

From site/components/Reveal.tsx (client island — wrap children, NÃO subir "use client" para a seção):
```typescript
// <Reveal delay={number}>{children}</Reveal> — entrada fade+sobe once-only, gate reduced-motion herdado.
```

Section shell pattern (de Resultados.tsx — RSC, sem "use client"):
```tsx
<section className="border-t border-line px-6 py-24 sm:py-32">
  <div className="mx-auto max-w-6xl">
    <Reveal><h2 className="font-display text-4xl uppercase leading-tight sm:text-5xl">…</h2></Reveal>
  </div>
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar as 4 constantes de conversão em lib/site.ts</name>
  <files>site/lib/site.ts</files>
  <read_first>
    - site/lib/site.ts (estado atual — padrões WHATSAPP_URL, STATS, TIMELINE a espelhar)
    - .planning/phases/02-convers-o-completa/02-PATTERNS.md (§ site/lib/site.ts — shapes exatos das constantes)
    - .planning/phases/02-convers-o-completa/02-CONTEXT.md (D-06, D-07, D-08, D-11, D-12, D-16, D-17, D-18, D-19)
  </read_first>
  <action>
    Adicionar 4 constantes ao final de `site/lib/site.ts`, espelhando o padrão `ReadonlyArray<T>` + comentário `// PLACEHOLDER` de STATS/TIMELINE e o builder de deeplink de WHATSAPP_URL (`"https://wa.me/" + N + "?text=" + encodeURIComponent(...)`).

    1. Constante de número compartilhado + builder de deeplink (D-07/D-08): `const WA_NUMBER = "0000000000"` comentado como PLACEHOLDER até o número real chegar (D-06/D-19); função `planDeeplink(planName: string): string` que retorna `"https://wa.me/" + WA_NUMBER + "?text=" + encodeURIComponent("Quero o Plano " + planName)`.

    2. `export const PLANS: ReadonlyArray<{ name: string; price: string; includes: ReadonlyArray<string>; whatsappUrl: string }>` com exatamente 2 objetos (D-05). Cada objeto: `name` "PLACEHOLDER A"/"PLACEHOLDER B", `price` `"Sob consulta"` (rótulo intencional on-brand — NUNCA a string crua "PLACEHOLDER" no campo price, Pitfall 5/UI-SPEC), `includes` `["PLACEHOLDER"]`, `whatsappUrl: planDeeplink("A")` / `planDeeplink("B")`. Comentário marcando que nomes/preços/inclusos reais chegam via chat (D-06).

    3. `export const TESTIMONIALS: ReadonlyArray<{ name: string; context: string; change: string; result: string; photo?: string; publishable: boolean }>` com exatamente 3 objetos (D-10), TODOS com `publishable: false` (D-11) — são placeholders que o gate editorial não renderiza. Campos de texto vazios `""`. Comentário: trocar por nomes reais via chat; sem nome real não publica.

    4. `export const COMMUNITY_WHATSAPP_URL = "https://chat.whatsapp.com/PLACEHOLDER"` (D-14/D-17) — link de convite do grupo exclusivo de alunos, SEPARADO de WHATSAPP_URL (não reutilizar o número de suporte). Comentário marcando placeholder até o link real chegar.

    5. `export const COMMUNITY_BENEFITS: ReadonlyArray<string>` com 3–4 strings (D-15) enquadrando a comunidade como parte do método ("não estar sozinho no processo") e reforçando exclusividade (entra quem contrata). Marcadas como PLACEHOLDER on-brand, sem hype.

    Nenhuma env var nova obrigatória (D-19) — todos os builds passam com placeholders. NÃO tocar em WHATSAPP_URL, STATS, TIMELINE.
  </action>
  <verify>
    <automated>cd site && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `site/lib/site.ts` contém `export const PLANS`, `export const TESTIMONIALS`, `export const COMMUNITY_WHATSAPP_URL`, `export const COMMUNITY_BENEFITS`
    - `PLANS` tem exatamente 2 elementos; cada um tem `whatsappUrl` contendo `wa.me/` e `encodeURIComponent` no builder
    - `TESTIMONIALS` tem exatamente 3 elementos, todos com `publishable: false`
    - Nenhum campo `price` contém a string literal `PLACEHOLDER` (usa "Sob consulta" ou "—")
    - `COMMUNITY_WHATSAPP_URL` contém `chat.whatsapp.com` e é distinto de `WHATSAPP_URL`
    - `cd site && npm run typecheck` sai com código 0
  </acceptance_criteria>
  <done>As 4 constantes existem com placeholders seguros; typecheck passa; nenhuma env var nova é obrigatória para o build.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar a seção Planos (RSC) e registrar em page.tsx</name>
  <files>site/components/sections/Planos.tsx, site/app/page.tsx</files>
  <read_first>
    - site/components/sections/Resultados.tsx (analog exato — heading + grid + map; o section shell e o grid)
    - site/components/ui/CTAButton.tsx (assinatura e hardening de link externo)
    - site/app/page.tsx (estado atual da ordem de seções e bloco de imports)
    - .planning/phases/02-convers-o-completa/02-UI-SPEC.md (§ Typography, § Color, § Visuals, § Responsive — preço Anton text-5xl/6xl é o anchor; distinção tonal D-09)
    - .planning/phases/02-convers-o-completa/02-PATTERNS.md (§ Planos.tsx — grid 2-col, card com CTA fixado mt-auto)
  </read_first>
  <action>
    Criar `site/components/sections/Planos.tsx` como RSC (SEM "use client" — DSGN-05; só Reveal/CTAButton carregam client). Imports: `Reveal` de `@/components/Reveal`, `CTAButton` de `@/components/ui/CTAButton`, `PLANS` de `@/lib/site`.

    Estrutura: section shell `border-t border-line px-6 py-24 sm:py-32` → `div mx-auto max-w-6xl` → `Reveal` com `<h2 className="font-display text-4xl uppercase leading-tight sm:text-5xl">` (título da seção on-brand, ex. "Escolha sua direção" ou similar sóbrio) → grade `grid gap-8 sm:grid-cols-2` (D-05, 2 cards) com `PLANS.map((p, i) => <Reveal key={p.name} delay={0.08 * i}>…card…</Reveal>)`.

    Cada card: `flex h-full flex-col border border-line p-8` com distinção tonal entre os dois cards (D-09 — discrição do executor: `bg-surface` no card "elevado" vs `bg-bg` no "base", OU espessura de borda). PROIBIDO: badge "popular", cor/hue, glow, estrelas. Hierarquia interna (UI-SPEC § Visuals): nome do plano (H3 — `font-body text-base font-semibold uppercase tracking-wider`) → preço (anchor — `font-display text-5xl uppercase leading-none sm:text-6xl`, renderiza `p.price`, que já é "Sob consulta"/"—", nunca "PLACEHOLDER" cru) → lista de inclusos como `<ul>`/`<li>` semântica mapeando `p.includes` → `CTAButton href={p.whatsappUrl} className="mt-auto"` fixado à base do card. Copy do CTA: `Quero o Plano {p.name}` (UI-SPEC § Copywriting; deeplink já carrega o mesmo texto). Nome acessível do CTA descreve o destino/plano.

    Tokens APENAS (nunca hex): `bg-bg`, `bg-surface`, `border-line`, `text-fg`, `text-muted`, `font-display`, `font-body`. Cards de altura igual via `flex flex-col` + `mt-auto` no CTA (UI-SPEC § Responsive).

    Em `site/app/page.tsx`: adicionar `import { Planos } from "@/components/sections/Planos";` ao bloco de imports e inserir `<Planos />` no `<main>` — nesta fase, imediatamente APÓS `<SobreRamon />` (a posição final D-01 é SobreRamon → Depoimentos → Comunidade → Planos → FAQ; Depoimentos e Comunidade entram no plano 02, que reordena para inserir antes de Planos). Para evitar conflito de ordenação, posicionar `<Planos />` entre `<SobreRamon />` e `<FAQ />` agora; o plano 02 insere Depoimentos e Comunidade antes de `<Planos />`.
  </action>
  <verify>
    <automated>cd site && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/sections/Planos.tsx` existe, NÃO contém a diretiva `"use client"`
    - Planos.tsx contém `import { PLANS } from "@/lib/site"` e `PLANS.map`
    - Planos.tsx contém `href={p.whatsappUrl}` (deeplink por plano, não WHATSAPP_URL genérico)
    - Planos.tsx contém `sm:grid-cols-2` (2 cards) e não contém a palavra `popular` nem nenhum hex `#`
    - Planos.tsx renderiza `p.price` com classe `font-display` (preço como anchor Anton)
    - `site/app/page.tsx` contém `import { Planos }` e `<Planos />` dentro de `<main>`
    - `cd site && npm run typecheck && npm run lint` saem com código 0
  </acceptance_criteria>
  <done>A seção Planos renderiza 2 cards monocromáticos com preço (anchor Anton), inclusos e CTA deeplink por plano; registrada em page.tsx; typecheck e lint passam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remover o bloco tracejado de placeholder de depoimentos em Resultados.tsx</name>
  <files>site/components/sections/Resultados.tsx</files>
  <read_first>
    - site/components/sections/Resultados.tsx (estado atual — bloco tracejado nas linhas ~28-36)
    - .planning/phases/02-convers-o-completa/02-PATTERNS.md (§ Resultados.tsx — escopo: deletar SÓ o bloco tracejado)
    - .planning/phases/02-convers-o-completa/02-RESEARCH.md (Pitfall 2 — duplicata se mantido)
  </read_first>
  <action>
    Remover de `site/components/sections/Resultados.tsx` APENAS o bloco de placeholder tracejado: o comentário `{/* PLACEHOLDER: depoimentos / antes-e-depois reais entram aqui... */}` e o `<Reveal delay={0.1}>` que o segue contendo o `div` com `border-dashed` e o texto "Depoimentos e transformações de alunos entram aqui". Esse bloco vira duplicata teaser da seção Depoimentos real (entregue no plano 02) duas seções antes — Pitfall 2.

    MANTER intacto: o `<section>`, o `div mx-auto max-w-6xl`, o `Reveal` do `<h2>` e toda a grade de `STATS` (linhas ~15-26) e o import de AnimatedCounter/STATS. Esta é uma deleção cirúrgica, não um redesign (consistente com D-03).
  </action>
  <verify>
    <automated>cd site && npm run typecheck && test "$(grep -c 'border-dashed' components/sections/Resultados.tsx)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/sections/Resultados.tsx` NÃO contém mais `border-dashed`
    - Resultados.tsx NÃO contém mais o texto "Depoimentos e transformações de alunos entram aqui"
    - Resultados.tsx AINDA contém `STATS.map` e a grade `sm:grid-cols-3` intacta
    - `cd site && npm run typecheck` sai com código 0
  </acceptance_criteria>
  <done>O bloco tracejado de placeholder foi removido; a grade de STATS permanece intacta; typecheck passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site → WhatsApp externo | CTAs abrem `wa.me`/convite de grupo em app/web de terceiro (saída de link, sem input do usuário) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CTA deeplink `wa.me/...?text=` em PLANS | mitigate | `encodeURIComponent()` no payload `?text=` (espelha o padrão WHATSAPP_URL existente); sanitiza o texto pré-preenchido |
| T-02-02 | Tampering | Reverse tabnabbing via `target="_blank"` nos CTAs de Plano | mitigate | Todo CTA usa `CTAButton`, que já aplica `rel="noopener noreferrer"` [VERIFIED]; nenhum `<a>` cru hand-rolled neste plano |
| T-02-03 | Information Disclosure | Placeholder cru vazando para produção | mitigate | Campo `price` renderiza rótulo intencional ("Sob consulta"/"—"), nunca a string `PLACEHOLDER`/`Lorem` na página (UI-SPEC § Copywriting) |
| T-02-SC | Tampering | npm installs | accept | Esta fase NÃO instala pacotes (RESEARCH § Package Legitimacy Audit: zero installs); nenhum checkpoint de legitimidade necessário |
</threat_model>

<verification>
- `cd site && npm run build` compila verde com os placeholders em lib/site.ts (D-19)
- Visual: seção Planos aparece com 2 cards, preço em destaque (Anton), inclusos e CTA por card
- Visual: Resultados não tem mais o bloco tracejado de depoimentos
- Monocromático estrito: nenhum hex hardcoded; nenhum badge "popular"
- Foco e reduced-motion herdados via CTAButton/Reveal
</verification>

<success_criteria>
- PLANS, TESTIMONIALS, COMMUNITY_BENEFITS, COMMUNITY_WHATSAPP_URL existem em lib/site.ts com placeholders seguros
- Seção Planos renderiza 2 cards sóbrios com preço, inclusos e CTA deeplink por plano
- Planos registrado em page.tsx; bloco tracejado removido de Resultados.tsx
- build, lint e typecheck passam
</success_criteria>

<output>
Create `.planning/phases/02-convers-o-completa/02-01-SUMMARY.md` when done
</output>
