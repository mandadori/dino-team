---
phase: 02-convers-o-completa
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - site/lib/site.ts
autonomous: true
requirements: [CONF-01]
must_haves:
  truths:
    - "planDeeplink() em site/lib/site.ts deriva WA_NUMBER de process.env.NEXT_PUBLIC_WHATSAPP_URL quando a env var estiver definida"
    - "Quando NEXT_PUBLIC_WHATSAPP_URL é 'https://wa.me/55119XXXXXXX?text=...', WA_NUMBER extrai '55119XXXXXXX'"
    - "Quando NEXT_PUBLIC_WHATSAPP_URL é undefined, WA_NUMBER cai em '0000000000' (comportamento de placeholder inalterado)"
    - "WHATSAPP_URL e planDeeplink() usam o mesmo número base — não há inconsistência entre CTAs de Plano e os demais CTAs"
    - "O build, lint e typecheck passam sem alterar nenhum outro arquivo"
  artifacts:
    - path: "site/lib/site.ts"
      provides: "planDeeplink() lendo NEXT_PUBLIC_WHATSAPP_URL"
      contains: "process.env.NEXT_PUBLIC_WHATSAPP_URL"
  key_links:
    - from: "site/components/sections/Planos.tsx"
      to: "site/lib/site.ts"
      via: "href={p.whatsappUrl} → planDeeplink() → WA_NUMBER derivado do env var"
      pattern: "NEXT_PUBLIC_WHATSAPP_URL"
---

## Phase Goal

**As a** operador do site, **I want to** configurar `NEXT_PUBLIC_WHATSAPP_URL` em produção e ter todos os CTAs de WhatsApp — incluindo os deeplinks por plano — apontando para o número real, **so that** nenhum visitante seja direcionado ao placeholder `wa.me/0000000000` após o deploy.

> Slice deste plano (gap closure): corrige a inconsistência de CONF-01 — `planDeeplink()` passa a ler `NEXT_PUBLIC_WHATSAPP_URL`, espelhando o padrão já em uso por `WHATSAPP_URL`. Nenhum outro arquivo é tocado.

<objective>
Corrigir `planDeeplink()` em `site/lib/site.ts` para derivar `WA_NUMBER` de `process.env.NEXT_PUBLIC_WHATSAPP_URL`, eliminando a inconsistência com `WHATSAPP_URL` e satisfazendo CONF-01 completamente.
</objective>

<tasks>

<task id="02-03-T01" type="execute">
  <title>Derivar WA_NUMBER de NEXT_PUBLIC_WHATSAPP_URL em planDeeplink()</title>

  <read_first>
    - site/lib/site.ts — ler o arquivo inteiro antes de editar; focar nas linhas 7–11 (padrão de WHATSAPP_URL) e 37–50 (WA_NUMBER + planDeeplink)
  </read_first>

  <action>
Em `site/lib/site.ts`, substituir a linha 39:

```
const WA_NUMBER = "0000000000";
```

pelo bloco de extração que espelha o padrão de WHATSAPP_URL:

```
const _waBase = process.env.NEXT_PUBLIC_WHATSAPP_URL ?? "";
const WA_NUMBER = _waBase.match(/wa\.me\/([^?]+)/)?.[1] ?? "0000000000";
```

O comentário acima do bloco (linhas 37–38) deve ser atualizado para refletir que WA_NUMBER agora lê o env var:

```
// Número extraído de NEXT_PUBLIC_WHATSAPP_URL (espelha WHATSAPP_URL).
// Fallback: "0000000000" enquanto o env var não estiver configurado (D-06/D-19).
```

Nenhum outro trecho do arquivo é alterado.
  </action>

  <acceptance_criteria>
    - site/lib/site.ts contém `process.env.NEXT_PUBLIC_WHATSAPP_URL` na definição de WA_NUMBER (grep confirma)
    - site/lib/site.ts contém `_waBase.match(/wa\\.me\\/([^?]+)/)` (regex de extração presente)
    - A linha `const WA_NUMBER = "0000000000";` (hardcoded, sem env var) não existe mais no arquivo
    - `cd site && npx tsc --noEmit` sai com código 0
    - `cd site && npx next build` sai com código 0 (build passa com a mudança e placeholders existentes)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep -n "NEXT_PUBLIC_WHATSAPP_URL" site/lib/site.ts` — deve retornar pelo menos 2 linhas: a de WHATSAPP_URL (já existia) e a nova de WA_NUMBER
2. `grep -n 'WA_NUMBER = "0000000000"' site/lib/site.ts` — não deve retornar nenhum resultado (hardcoded removido)
3. `grep -n "_waBase" site/lib/site.ts` — deve retornar a linha de extração do regex
4. `cd site && npx tsc --noEmit` — deve sair 0 (typecheck limpo)
5. Inspecionar `planDeeplink("A")` logicamente: com `NEXT_PUBLIC_WHATSAPP_URL=https://wa.me/5511999999999?text=Quero+minha+consultoria`, `_waBase.match(...)` extrai `5511999999999`; `planDeeplink("A")` retorna `https://wa.me/5511999999999?text=Quero+o+Plano+A` — consistente com `WHATSAPP_URL`
</verification>

<success_criteria>
- `site/lib/site.ts` linha de WA_NUMBER lê `process.env.NEXT_PUBLIC_WHATSAPP_URL` com fallback `"0000000000"`
- TypeScript compila sem erros
- Build Next.js passa
- Nenhum outro arquivo foi modificado
</success_criteria>
