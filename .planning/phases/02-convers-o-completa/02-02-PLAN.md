---
phase: 02-convers-o-completa
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - site/components/sections/Depoimentos.tsx
  - site/components/sections/Comunidade.tsx
  - site/components/sections/CtaFinal.tsx
  - site/app/page.tsx
autonomous: true
requirements: [CONV-02, CONV-03, CONF-01]
must_haves:
  truths:
    - "Um visitante lê depoimentos nomeados estruturados como contexto→mudança→resultado, e os placeholders (publishable:false) nunca aparecem"
    - "Um visitante sem foto de aluno vê um avatar placeholder monocromático intencional, não uma imagem quebrada"
    - "Um visitante encontra a seção Comunidade enquadrada como parte do método, com 3–4 benefícios e um CTA para o grupo exclusivo de alunos (COMMUNITY_WHATSAPP_URL, não o número de suporte)"
    - "A ordem final da landing é Hero → ParaQuemE → Metodo → Resultados → SobreRamon → Depoimentos → Comunidade → Planos → FAQ → CtaFinal (D-01)"
    - "O CtaFinal continua como reforço emocional com copy distinta dos CTAs de Plano (D-04)"
  artifacts:
    - path: "site/components/sections/Depoimentos.tsx"
      provides: "Seção Depoimentos RSC com gate publishable e avatar placeholder"
      contains: "filter"
      min_lines: 25
    - path: "site/components/sections/Comunidade.tsx"
      provides: "Seção Comunidade RSC com benefícios + CTA de grupo"
      contains: "COMMUNITY_WHATSAPP_URL"
      min_lines: 20
    - path: "site/app/page.tsx"
      provides: "3 novas seções na ordem D-01"
      contains: "<Depoimentos />"
  key_links:
    - from: "site/components/sections/Depoimentos.tsx"
      to: "site/lib/site.ts"
      via: "filter(t => t.publishable)"
      pattern: "filter\\(.*publishable"
    - from: "site/components/sections/Comunidade.tsx"
      to: "COMMUNITY_WHATSAPP_URL"
      via: "CTAButton href de convite do grupo"
      pattern: "href=\\{COMMUNITY_WHATSAPP_URL\\}"
---

## Phase Goal

**As a** visitante interessado em consultoria, **I want to** ver provas sociais nomeadas e entender a comunidade de alunos como parte do método, **so that** eu confio que não vou estar sozinho no processo antes de fechar.

> Slice deste plano (wave 2): prova social (Depoimentos) + pertencimento (Comunidade), mais a reordenação final D-01 e a reconciliação do CtaFinal — após este plano o funil de conversão está completo ponta a ponta.

<objective>
Completar o funil com o slice de prova social + pertencimento: Depoimentos nomeados (com gate editorial "sem nome real, não publica") e Comunidade enquadrada como parte do método. Reordenar a landing para a sequência final D-01 e reconciliar a copy do CtaFinal.

Purpose: CONV-02 (Depoimentos contexto→mudança→resultado) + CONV-03 (Comunidade como parte do método, sem gamificação) + conclusão de CONF-01 (CTA da Comunidade usa COMMUNITY_WHATSAPP_URL distinto).

Output: `Depoimentos.tsx` e `Comunidade.tsx` (RSC), 3 seções inseridas em `page.tsx` na ordem D-01, e copy do `CtaFinal.tsx` confirmada como reforço emocional distinto dos cards de Plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-convers-o-completa/02-CONTEXT.md
@.planning/phases/02-convers-o-completa/02-UI-SPEC.md
@.planning/phases/02-convers-o-completa/02-PATTERNS.md
@brand/brand-book.md
@brand/tom-de-voz.md
@brand/publico-alvo.md
@.planning/phases/02-convers-o-completa/02-01-SUMMARY.md

<interfaces>
<!-- Constantes criadas no plano 02-01 (lib/site.ts) que este plano consome: -->

```typescript
export const TESTIMONIALS: ReadonlyArray<{
  name: string; context: string; change: string; result: string; photo?: string; publishable: boolean;
}>;
export const COMMUNITY_WHATSAPP_URL: string; // link de convite do grupo de alunos (NÃO o número de suporte)
export const COMMUNITY_BENEFITS: ReadonlyArray<string>;
```

From site/components/ui/CTAButton.tsx:
```typescript
export function CTAButton(props: { href: string; children: ReactNode; variant?: "primary" | "outline"; className?: string }): JSX.Element;
// já aplica target="_blank" + rel="noopener noreferrer" + focus-ring + hover:scale reduced-motion-safe
```

From site/components/RamonPhoto.tsx (precedente do avatar placeholder monocromático — branch src-ausente):
```tsx
<div role="img" aria-label={alt} className="flex items-center justify-center overflow-hidden border border-line bg-surface ...">
  <span aria-hidden="true" className="font-display uppercase tracking-widest text-muted">…</span>
</div>
```

Section shell (RSC, sem "use client"):
```tsx
<section className="border-t border-line px-6 py-24 sm:py-32">
  <div className="mx-auto max-w-6xl">…</div>   {/* max-w-4xl text-center para Comunidade */}
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar a seção Depoimentos (RSC) com gate publishable e avatar placeholder</name>
  <files>site/components/sections/Depoimentos.tsx</files>
  <read_first>
    - site/components/sections/SobreRamon.tsx (analog — grid + map; precedente de RamonPhoto)
    - site/components/sections/Resultados.tsx (analog — section shell + grid-of-cards)
    - site/components/RamonPhoto.tsx (branch src-ausente — copiar o padrão do avatar monocromático)
    - .planning/phases/02-convers-o-completa/02-PATTERNS.md (§ Depoimentos.tsx — gate, grid, avatar)
    - .planning/phases/02-convers-o-completa/02-UI-SPEC.md (§ Copywriting empty state; § Accessibility; § Color cards bg-surface)
    - .planning/phases/02-convers-o-completa/02-RESEARCH.md (Pitfall 1 — grade vazia é o comportamento correto na era de placeholder)
  </read_first>
  <action>
    Criar `site/components/sections/Depoimentos.tsx` como RSC (SEM "use client"). Imports: `Reveal` de `@/components/Reveal`, `TESTIMONIALS` de `@/lib/site`. (next/image só se uma foto real chegar; o placeholder não precisa de imagem.)

    Gate editorial (D-11): no topo, `const visible = TESTIMONIALS.filter((t) => t.publishable);`. Na era de placeholder todos têm `publishable: false` → `visible.length === 0` → grade vazia. Renderizar o heading da seção + grade vazia; SEM "em breve", SEM skeleton, SEM card vazio (UI-SPEC § Copywriting empty state, Pitfall 1). Adicionar comentário documentando que grade vazia é o comportamento correto até nomes reais chegarem.

    Estrutura: section shell `border-t border-line px-6 py-24 sm:py-32` → `div mx-auto max-w-6xl` → `Reveal` `<h2 className="font-display text-4xl uppercase leading-tight sm:text-5xl">` (título on-brand) → grade `grid gap-px ... sm:grid-cols-3` (D-13 — 3 colunas desktop, empilhado mobile) com `visible.map((t, i) => <Reveal key={t.name} delay={0.08 * i}>…card…</Reveal>)`.

    Cada card (`bg-surface border-line p-8`): avatar (opcional) + nome em CAPS (`font-body text-sm font-semibold uppercase tracking-wider text-fg`) + parágrafo único em 3 movimentos legíveis contexto→mudança→resultado (`t.context`, `t.change`, `t.result`) em `font-body text-base text-muted` (UI-SPEC § Typography/Color — `text-muted` sobre `bg-surface` passa AA; NUNCA `#7f7f7f` sobre branco). Sem aspas decorativas grandes, sem estrelas.

    Avatar (D-12): se `t.photo` ausente → avatar placeholder monocromático intencional copiando o branch src-ausente de RamonPhoto: `<div role="img" aria-label={t.name} className="flex items-center justify-center overflow-hidden border border-line bg-surface ...">` com `<span aria-hidden="true" className="font-display uppercase tracking-widest text-muted">` (inicial do nome ou glyph neutro). Se `t.photo` presente → `next/image` com `object-cover grayscale contrast-125` e `alt` descritivo. Deliberado, nunca quebrado.

    Tokens APENAS, nunca hex. Acessibilidade: avatar placeholder decorativo `aria-hidden`/`role="img"+aria-label` conforme UI-SPEC § Accessibility.
  </action>
  <verify>
    <automated>cd site && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/sections/Depoimentos.tsx` existe, NÃO contém `"use client"`
    - Depoimentos.tsx contém `TESTIMONIALS.filter` filtrando por `publishable`
    - Depoimentos.tsx contém `sm:grid-cols-3` (D-13) e NÃO contém nenhum hex `#` nem a string "em breve"/"skeleton"
    - Depoimentos.tsx renderiza `t.context`, `t.change`, `t.result` (estrutura contexto→mudança→resultado)
    - Avatar placeholder usa `role="img"` + `aria-label` + `bg-surface` (precedente RamonPhoto)
    - `cd site && npm run typecheck && npm run lint` saem com código 0
  </acceptance_criteria>
  <done>Depoimentos renderiza só itens publishable (grade vazia na era de placeholder), com avatar monocromático intencional e estrutura contexto→mudança→resultado; typecheck e lint passam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar a seção Comunidade (RSC) com benefícios + CTA de grupo</name>
  <files>site/components/sections/Comunidade.tsx</files>
  <read_first>
    - site/components/sections/CtaFinal.tsx (analog — bloco centralizado max-w-4xl + CTA único)
    - site/components/sections/SobreRamon.tsx (analog — map de lista de benefícios)
    - site/components/ui/CTAButton.tsx (assinatura + hardening de link)
    - .planning/phases/02-convers-o-completa/02-PATTERNS.md (§ Comunidade.tsx — shell centralizado, benefits map, CTA de grupo)
    - .planning/phases/02-convers-o-completa/02-UI-SPEC.md (§ Copywriting CTA Comunidade "Entrar na comunidade"; § Accessibility lista semântica)
    - .planning/phases/02-convers-o-completa/02-CONTEXT.md (D-14, D-15, D-16, D-17 — grupo exclusivo de alunos, sem gamificação)
  </read_first>
  <action>
    Criar `site/components/sections/Comunidade.tsx` como RSC (SEM "use client"). Imports: `Reveal` de `@/components/Reveal`, `CTAButton` de `@/components/ui/CTAButton`, `COMMUNITY_BENEFITS` e `COMMUNITY_WHATSAPP_URL` de `@/lib/site`.

    Estrutura: section shell `border-t border-line px-6 py-24 sm:py-32` → `div mx-auto max-w-4xl text-center` (bloco textual centralizado, D-15) → `Reveal` `<h2 className="font-display text-4xl uppercase leading-tight sm:text-5xl">` (título on-brand enquadrando a comunidade como parte do método) → parágrafo introdutório opcional `font-body text-muted` → lista semântica `<ul>` mapeando `COMMUNITY_BENEFITS` como `<li>` (3–4 benefícios textuais, UI-SPEC § Accessibility exige `<ul>`/`<li>`), cada um em `Reveal delay={0.06 * i}` com `font-body text-muted` → `Reveal` final com `<div className="mt-10 flex justify-center">` contendo `<CTAButton href={COMMUNITY_WHATSAPP_URL}>Entrar na comunidade</CTAButton>`.

    CRÍTICO (D-14/D-17, Pitfall 4): o CTA usa `COMMUNITY_WHATSAPP_URL` (link de convite do grupo exclusivo de alunos), NUNCA `WHATSAPP_URL` (suporte) nem um deeplink de plano. A copy reforça exclusividade — acesso de quem contrata, não convite a qualquer visitante. Sem gamificação, sem contador de membros, sem quote de membro (D-15). Nome acessível do link: "Entrar na comunidade".

    Tokens APENAS, nunca hex.
  </action>
  <verify>
    <automated>cd site && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `site/components/sections/Comunidade.tsx` existe, NÃO contém `"use client"`
    - Comunidade.tsx contém `import` de `COMMUNITY_BENEFITS` e `COMMUNITY_WHATSAPP_URL` de `@/lib/site`
    - Comunidade.tsx contém `href={COMMUNITY_WHATSAPP_URL}` e NÃO referencia `WHATSAPP_URL`
    - Comunidade.tsx usa `<ul>` e `<li>` (lista semântica de benefícios) e `max-w-4xl`
    - Comunidade.tsx NÃO contém nenhum hex `#` nem palavra de gamificação (contador/membros/badge)
    - `cd site && npm run typecheck && npm run lint` saem com código 0
  </acceptance_criteria>
  <done>Comunidade renderiza título + 3–4 benefícios em lista semântica + CTA para COMMUNITY_WHATSAPP_URL, sem gamificação; typecheck e lint passam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reordenar page.tsx para a sequência D-01 e reconciliar copy do CtaFinal</name>
  <files>site/app/page.tsx, site/components/sections/CtaFinal.tsx</files>
  <read_first>
    - site/app/page.tsx (estado atual após plano 02-01 — Planos já registrado entre SobreRamon e FAQ)
    - site/components/sections/CtaFinal.tsx (copy atual — "Consistência vence. Direção define." + CTA "Quero minha direção")
    - .planning/phases/02-convers-o-completa/02-CONTEXT.md (D-01, D-04 — ordem final; CtaFinal vira reforço emocional)
    - .planning/phases/02-convers-o-completa/02-RESEARCH.md (Pitfall 3 — D-04 é reconciliação, NÃO rewrite from-scratch)
    - .planning/phases/02-convers-o-completa/02-UI-SPEC.md (§ Copywriting CtaFinal — sign-off "O topo exige direção." opcional)
    - brand/tom-de-voz.md (tom para a copy de reforço)
  </read_first>
  <action>
    Em `site/app/page.tsx`: adicionar imports de `Depoimentos` e `Comunidade` ao bloco de imports (`import { Depoimentos } from "@/components/sections/Depoimentos";` e `import { Comunidade } from "@/components/sections/Comunidade";`). No `<main>`, inserir `<Depoimentos />` e `<Comunidade />` ANTES de `<Planos />` (que o plano 02-01 já colocou entre `<SobreRamon />` e `<FAQ />`), produzindo a ordem final D-01 exata:
    `Hero → ParaQuemE → Metodo → Resultados → SobreRamon → Depoimentos → Comunidade → Planos → FAQ → CtaFinal`.

    Em `site/components/sections/CtaFinal.tsx` (D-04 é RECONCILIAÇÃO, não rewrite — Pitfall 3): a copy atual já é reforço emocional on-brand ("Consistência vence. Direção define." / subcopy / CTA "Quero minha direção" → `WHATSAPP_URL`). Confirmar que: (a) a copy do bloco permanece de identidade/reforço, distinta dos cards de Plano; (b) o texto do CTA difere de "Quero o Plano X" (já difere: "Quero minha direção"); (c) o `href` permanece `WHATSAPP_URL` (suporte, NÃO um deeplink de plano). NÃO deletar a linha forte existente sem uma substituição on-brand claramente melhor. Opcionalmente adicionar o sign-off `O topo exige direção.` como fecho (UI-SPEC § Copywriting). Estrutura (section/Reveal/CTAButton) permanece intacta.
  </action>
  <verify>
    <automated>cd site && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `site/app/page.tsx` contém imports de `Depoimentos`, `Comunidade` e `Planos`
    - Em page.tsx, dentro de `<main>`, a ordem textual é `<SobreRamon />` → `<Depoimentos />` → `<Comunidade />` → `<Planos />` → `<FAQ />` → `<CtaFinal />`
    - `site/components/sections/CtaFinal.tsx` ainda usa `href={WHATSAPP_URL}` (suporte, não deeplink de plano)
    - CtaFinal.tsx mantém copy de reforço emocional e CTA distinto de "Quero o Plano"
    - `cd site && npm run build` sai com código 0 (compila verde com placeholders, D-19)
  </acceptance_criteria>
  <done>A landing tem a ordem final D-01; o CtaFinal é reforço emocional com CTA de suporte distinto dos planos; build verde com placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site → WhatsApp externo | CTAs de Comunidade (convite de grupo) e CtaFinal (suporte) abrem `wa.me`/`chat.whatsapp.com` em terceiro; sem input do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Reverse tabnabbing via `target="_blank"` no CTA da Comunidade e CtaFinal | mitigate | CTA usa `CTAButton`, que já aplica `rel="noopener noreferrer"` [VERIFIED]; nenhum `<a>` cru hand-rolled |
| T-02-05 | Information Disclosure | Depoimento placeholder (publishable:false) vazando para a página | mitigate | Gate `.filter(t => t.publishable)` no render (D-11); placeholders nunca renderizam; sem "em breve"/skeleton |
| T-02-06 | Information Disclosure | CTA da Comunidade apontando para o número de suporte em vez do grupo | mitigate | `href={COMMUNITY_WHATSAPP_URL}` — constante separada (D-14/D-17, Pitfall 4); acceptance criteria verifica ausência de `WHATSAPP_URL` em Comunidade.tsx |
| T-02-SC | Tampering | npm installs | accept | Esta fase NÃO instala pacotes (RESEARCH § Package Legitimacy Audit: zero installs); nenhum checkpoint necessário |
</threat_model>

<verification>
- `cd site && npm run build` compila verde com placeholders (D-19)
- Visual: ordem final D-01 na home (Depoimentos → Comunidade → Planos entre SobreRamon e FAQ)
- Visual: Depoimentos com grade vazia (era de placeholder, correto); Comunidade com benefícios + CTA de grupo
- CtaFinal continua reforço emocional com CTA distinto dos planos
- Monocromático estrito; sem gamificação; foco/reduced-motion herdados
</verification>

<success_criteria>
- Depoimentos renderiza só publishable (contexto→mudança→resultado), avatar monocromático intencional
- Comunidade enquadrada como parte do método, CTA para COMMUNITY_WHATSAPP_URL distinto
- page.tsx na ordem final D-01; CtaFinal reconciliado como reforço emocional
- build, lint e typecheck passam
</success_criteria>

<output>
Create `.planning/phases/02-convers-o-completa/02-02-SUMMARY.md` when done
</output>
